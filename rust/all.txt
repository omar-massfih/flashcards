What is Cargo in the Rust ecosystem?	The included dependency manager and build tool.
What does the `!` in `println!` indicate?	It’s a macro invocation, not a normal function call.
What does `cargo new <name>` do?	Creates a new project directory with a starter Rust program and Cargo config.
What two top-level items does Cargo generate in a new binary project (besides the `src` dir)?	`Cargo.toml` and a `src/` directory (with `main.rs` inside `src/`).
Where does Cargo expect Rust source files to live?	Inside the `src/` directory.
What is `Cargo.toml`?	The project’s Cargo configuration file.
How do you build a Cargo project?	Run `cargo build`.
What does `cargo check` do?	Checks that code compiles but does not produce an executable.
Why use `cargo check` during development?	It’s usually faster feedback than a full build.
What is `Cargo.lock` for?	It records the exact versions of dependencies used in the project.
How do you build with optimizations for release?	Run `cargo build --release`.
What’s the trade-off of `cargo build --release`?	Faster runtime, but slower compile times.
What does `loop { ... }` do?	Creates an infinite loop that runs until you exit it (e.g., with `break`).
What is the Rust prelude?	A set of standard-library items automatically in scope in every program.
When do you write a `use ...;` statement?	To bring a module/type/trait into scope when it isn’t already available (e.g., not in the prelude) or to shorten paths.
What is `String` (std) in one phrase?	A growable, UTF-8–encoded owned string.
What does `&mut T` mean?	A mutable reference to a value of type `T` (allows mutation through the reference).
Why pass `&mut T` to a function?	So the function can mutate a caller-owned value without taking ownership of it.
What does `read_line(&mut String)` return?	A `Result` indicating success (`Ok(...)`) or failure (`Err(...)`).
What is `Result<T, E>`?	An enum that represents either success (`Ok(T)`) or failure (`Err(E)`).
What does the wildcard pattern `_` mean?	Match anything (catch-all), ignoring the matched value.
What does `parse::<T>()` do?	Attempts to convert a string to type `T`, returning a `Result`.
Why add a type annotation like `: u32`?	To guide type inference and choose the concrete type you want.
What is shadowing?	Re-declaring a name to bind it to a new value (possibly a different type) in the same scope.
Why can’t you compare values of different types directly?	Operations like comparison require compatible types; Rust forces explicit conversions instead of implicit type mixing.
What does `cmp(&other)` return?	An `Ordering` describing how two values compare.
Difference between `start..end` and `start..=end`?	`start..end` excludes `end`; `start..=end` includes `end`.
What is “turbofish” syntax `::<T>` used for?	To specify generic type parameters explicitly, e.g., `parse::<u32>()`.
When does Rust catch attempts to reassign an immutable binding?	At compile time (a compile-time error).
Why does Rust default to immutable bindings?	It nudges you toward safer, easier-to-reason-about code and supports safe concurrency.
What kind of bug can immutability help prevent?	Code that assumes a value won’t change being broken by code that changes it (especially intermittently).
What must a Rust `const` declaration always include?	A type annotation (e.g., `const X: u32 = ...;`).
Where can Rust constants be declared?	In any scope, including global scope.
What is the “constant expression” restriction for constants?	A `const` can’t depend on values only known at runtime.
Why replace repeated “magic numbers” with a `const`?	Conveys meaning; one place to update if the value changes.
What is shadowing in Rust (in one phrase)?	Rebinding the same name with `let`, creating a new binding that hides the previous one.
Shadowing vs `mut`: what’s the key difference in how the value changes?	Shadowing creates a new binding (requires `let`); `mut` allows reassignment of the same binding.
Shadowing: why does it help prevent accidental reassignment?	Changing the value requires `let`; an assignment without `let` won’t compile for an immutable binding.
Shadowing vs `mut`: can shadowing change the type?	Yes; shadowing can change the type while reusing the same name.
Why can’t `mut` be used to “change” a variable’s type?	A binding’s type is fixed; assignment requires a value of the same type.
How does shadowing interact with inner scopes?	An inner block can shadow a name temporarily; when the block ends, the outer binding is visible again.
Why might you choose shadowing over new variable names?	It avoids awkward names (e.g., `spaces_str`/`spaces_num`) while keeping code readable.
Integer types: what do `i*` vs `u*` mean (Rust)?	`i*` is signed (can be negative); `u*` is unsigned (nonnegative).
What does a type like `u32` indicate?	An unsigned 32-bit integer.
What built-in integer sizes does Rust provide?	8/16/32/64/128-bit signed+unsigned plus `isize`/`usize`.
What determines the size of `isize`/`usize`?	The target architecture (64-bit on 64-bit platforms; 32-bit on 32-bit platforms).
What integer type does Rust default to when unspecified?	`i32`.
When is `usize` commonly used in Rust?	For indexing collections/arrays and sizes/lengths.
How can you write integer literals in hex/octal/binary (Rust)?	Hex `0x...`; octal `0o...`; binary `0b...`; decimal is the default.
What is a numeric type suffix on a literal?	A suffix like `57u8` that explicitly selects the literal’s type.
What is integer overflow (Rust)?	When a value exceeds the range a fixed-size integer type can represent.
What happens on integer overflow in debug builds (Rust)?	Runtime overflow checks cause a panic when overflow occurs.
What happens on integer overflow in release builds by default (Rust)?	It wraps using two’s complement wrapping (no overflow panic by default).
What are the four method families for handling overflow explicitly?	`wrapping_*`; `checked_*`; `overflowing_*`; `saturating_*`.
What do `wrapping_*` methods do?	Always wrap on overflow (in all build modes).
What do `checked_*` methods do on overflow?	Return `None` (an `Option` indicating overflow).
What do `overflowing_*` methods return?	The wrapped value and a boolean indicating whether overflow happened.
What do `saturating_*` methods do?	Clamp results at the type’s min/max instead of wrapping.
What floating-point type does Rust default to?	`f64`.
How do you write a `char` literal vs a string literal (Rust)?	`char` uses single quotes (e.g., `'z'`); strings use double quotes (e.g., "z").
What is a tuple (Rust)?	A fixed-length compound type that can hold values of different types.
How do you destructure a tuple (Rust)?	Use a pattern: `let (x, y, z) = tup;`.
How do you access tuple elements by index (Rust)?	Use dot-indexing: `tup.0`, `tup.1`, etc.
What is the unit type/value in Rust?	`()`; an empty value / empty return type.
What is an array in Rust (core properties)?	A fixed-length collection where every element has the same type.
Arrays vs vectors: when should you prefer `Vec<T>` over arrays?	When you need a growable/shrinkable collection; if unsure, use a vector.
What happens on out-of-bounds array indexing in Rust (and why)?	It panics at runtime via bounds checks to prevent invalid memory access (memory safety).
In Rust function signatures, what must you write for each parameter?	The parameter’s type.
Why does Rust require parameter types in function signatures?	It helps the compiler produce better errors and reduces the need for type annotations elsewhere.
What is a statement (Rust, concept)?	An instruction that performs an action and does not return a value.
What is an expression (Rust, concept)?	Something that evaluates to a value.
Is `let x = 5;` a statement or an expression in Rust?	A statement.
Why can’t you write `let x = (let y = 6);` in Rust?	`let` is a statement and doesn’t produce a value to assign.
Is calling a function a statement or an expression in Rust?	An expression (it evaluates to a value).
In a block expression, what determines the block’s value?	The value of its final expression (the “tail expression”).
What is a “tail expression” in Rust?	The final expression in a block without a trailing semicolon; it becomes the block’s value.
What does adding a semicolon to an expression do in Rust?	It turns the expression into a statement, so it no longer yields its value.
In a block, what happens if the last line ends with `;`?	The block’s value becomes `()` (unit), because there is no tail expression.
How do you declare a function return type in Rust?	With `-> T` after the parameter list.
How do Rust functions typically return a value without `return`?	By making the last line of the body a tail expression (no semicolon).
What common mistake causes a function to return `()` unexpectedly?	Putting a semicolon after the intended return expression (removing the tail expression).
Why does `x + 1;` often cause a return-type mismatch in Rust functions?	The semicolon makes it a statement, so it yields `()` instead of the numeric value.
What kind of compiler error often appears when a function’s body yields `()` but the signature says it returns a non-`()` type?	A “mismatched types” error (expected the declared return type, found `()`).
How can a block expression be used with `let`?	You can bind a block’s value: `let x = { ... tail_expression ... };`.
What is an `if` expression used for (Rust)?	Branching: run a block when a condition is true; optionally run an `else` block when false.
What type must a Rust `if` condition have?	`bool`.
Does Rust perform truthy/falsy coercions for `if` conditions?	No; conditions must be explicitly boolean.
Why can “too many `else if`” be a code smell (per TRPL)?	It can clutter code; consider refactoring (e.g., using `match` for complex branching).
Is `if` a statement or an expression in Rust?	An expression (it can produce a value).
What does “`if` is an expression” enable in Rust?	Using `if` on the right-hand side of `let` to choose a value.
When using `if` in `let`, what must be true about the result types of each arm?	All possible arm results must have the same type.
Why must those arm result types match (Rust)?	A variable has a single compile-time type; Rust must know it definitively.
What common compile error happens when `if` and `else` produce different types?	An error that the `if` and `else` have incompatible types (type mismatch).
What problem does `if let` solve (Rust)?	Run code for one matching pattern without writing a full `match` with boilerplate like `_ => ()`.
In `if let PATTERN = EXPR`, what separates the pattern and the expression (Rust)?	An equals sign: `if let <pattern> = <expr> { ... }`.
In `if let Some(x) = opt { ... }`, where is `x` in scope (Rust)?	Only inside the `if let` block (and its `else` is a separate scope).
What tradeoff do you make by using `if let` instead of `match` (Rust)?	Conciseness vs losing `match`’s exhaustiveness checking.
What is `let...else` used for (Rust)?	Pattern-match to bind names in the outer scope on success; otherwise run an `else` that exits the current flow (“happy path”).
What is the basic syntax shape of `let...else` (Rust)?	`let PATTERN = EXPR else { ... };`
If the pattern matches in `let...else`, where do bindings live (Rust)?	In the surrounding scope after the `let...else` statement (not inside a success block).
If the pattern does not match in `let...else`, what must the `else` block do (Rust)?	It must diverge (e.g., `return`, `break`, `continue`, or `panic!`), because execution can’t continue with missing bindings.
Why does `let...else` “stay on the happy path” (Rust)?	The main logic continues unindented after the binding; non-matching cases exit early in `else`.
When is `let...else` a good fit versus `if let { ... } else { return ... }` (Rust)?	When you want to bind a value for later code and handle the non-match with an early-exit `else` (clearer control flow).
How can a `loop` produce a value (Rust)?	Use `break <expr>`; the loop expression evaluates to that value.
Where does the value from `break <expr>` go?	It becomes the value of the entire `loop` expression (often bound with `let result = loop { ... };`).
In nested loops, which loop do `break` and `continue` affect by default?	The innermost loop at that point.
What is a loop label used for?	Disambiguating which loop `break`/`continue` should apply to in nested loops.
What is the syntax shape of a labeled loop?	`'label: loop { ... }` (label starts with a single quote).
How do you break an outer labeled loop from inside an inner loop?	`break 'label;`.
Why does `while` reduce nesting versus `loop` + `if` + `break` patterns?	The condition is built into the loop construct, so it’s clearer and less nested.
Why is iterating with `while` + manual index considered error-prone?	Off-by-one mistakes can cause panics (out-of-bounds) or skipped elements.
Why can `while` + manual indexing also be slower?	It can require repeated bounds checks / comparisons each iteration.
Why is `for` preferred when iterating a collection (TRPL’s emphasis)?	Safer, more concise, and often more efficient.
What advantage does `for` have when the collection length changes?	You don’t need to update an index bound; it iterates the elements directly.
What is ownership (Rust)?	A set of rules that govern how a Rust program manages memory.
How does Rust enforce ownership rules?	With compile-time checks; code that violates the rules won’t compile.
Do ownership checks add runtime overhead (Rust)?	No; the checks happen at compile time.
What is the stack (memory) organized as?	Last in, first out (LIFO).
What must be true of data stored on the stack?	It must have a known, fixed size at compile time.
When must data be stored on the heap?	When its size is unknown at compile time or may change.
What happens when you allocate on the heap?	The allocator finds space, marks it used, and returns a pointer (address).
What are Rust’s three ownership rules?	Each value has an owner; only one owner at a time; when the owner goes out of scope, the value is dropped.
What does `String` enable compared to string literals (Rust)?	Owned, heap-allocated, growable text for runtime-sized content.
How do you create a `String` from a literal (Rust)?	`String::from("hello")`.
Why can a `String` be mutated but a string literal can’t (Rust)?	`String` owns heap-allocated data; literals are immutable data baked into the binary.
When is a `String`’s heap memory freed (Rust)?	When its owner goes out of scope and Rust runs `drop`.
What does `drop` do (Rust, conceptually)?	Runs cleanup for a value going out of scope (e.g., freeing heap memory).
What happens when you assign a `String` to another variable (Rust ownership)?	The value moves: stack metadata is copied, heap data is not; the source binding becomes invalid.
Why does Rust invalidate the source binding after a move?	To prevent double frees and other use-after-free style bugs.
Does Rust automatically deep-copy heap data on assignment?	No; implicit copies are intended to be inexpensive.
How do you deep-copy a `String` (Rust)?	Call `clone()` to copy the heap data (potentially expensive).
What does the `Copy` trait mean for assignment/passing (Rust)?	The value is trivially copied; the original remains valid.
Why can’t a type be `Copy` if it (or a field) implements `Drop`?	`Drop` implies special cleanup; `Copy` would allow duplicated values that both get dropped.
Which types commonly implement `Copy` (Rust)?	Integers; `bool`; floats; `char`; tuples of only `Copy` types (e.g., `(i32, i32)`).
How does passing a value to a function affect ownership (Rust)?	It moves or copies, just like assignment does.
What happens when a function takes a `String` parameter by value?	Ownership moves into the function; the caller can’t use it afterward.
What happens to ownership when a function returns a value?	The returned value moves to the caller.
What is a reference (Rust)?	An address you can follow to access data owned by another variable, without taking ownership.
What does “borrowing” mean (Rust)?	Creating a reference to use a value without taking ownership of it.
Why pass a reference to a function instead of an owned value (Rust)?	So the function can use the value without moving ownership; the caller can still use it afterward.
Can you mutate through a shared reference `&T` (Rust)?	No; shared references are immutable by default, so mutation through `&T` is rejected.
How do you allow a function to mutate a borrowed `String` (Rust)?	Take a mutable reference parameter, e.g. `fn f(s: &mut String) { ... }`.
What must be true to create `&mut s` (Rust)?	The binding must be mutable (e.g., `let mut s = ...;`).
What does a mutable reference communicate at the call site (Rust)?	That the function is allowed to mutate the borrowed value (`&mut` makes mutation explicit).
Core rule: how many mutable references can you have at once (Rust)?	At most one mutable reference to a value at a time.
Core rule: how many immutable references can you have at once (Rust)?	Any number of immutable references, as long as there’s no mutable reference in use.
Core rule: can you mix mutable and immutable references at the same time (Rust)?	No: you can’t have `&mut` while immutable borrows of the same value are still in use.
Why does Rust forbid multiple mutable references at the same time?	To prevent data races by ensuring controlled, exclusive mutation.
What is a data race (Rust explanation)?	When 2+ pointers access the same data at once, at least one writes, and there’s no synchronization.
How does Rust prevent data races related to references?	By enforcing borrowing rules at compile time (exclusive mutable access or shared read-only access).
How can you get two mutable references to the same value without overlap (Rust)?	Use scopes so the first mutable borrow ends before the second begins (e.g., an inner block).
When does a reference’s “active borrow scope” last (Rust)?	From creation through the last time the reference is used (not necessarily the end of the enclosing block).
Why can a mutable borrow be allowed after immutable borrows in the same scope (Rust)?	If the immutable borrows are no longer used, their borrow scopes have ended before the mutable borrow starts.
What’s a common symptom of trying to mutate through `&String`?	A compile error indicating you can’t borrow as mutable behind a `&` reference; use `&mut` instead.
What guarantee does Rust provide about references and dangling pointers?	References will never be dangling; the compiler enforces validity.
Why can’t a function return a reference to a local variable (Rust)?	The local is dropped when the function ends, so the returned reference would dangle.
What’s the typical fix for “returning a reference to a local” (Rust)?	Return an owned value instead (e.g., return `String`, not `&String`).
What does “references must always be valid” mean (Rust rule)?	A reference can’t outlive the data it points to.
What is a slice in Rust?	A reference to a contiguous sequence of elements in a collection (it does not own the data).
Why can returning a `usize` index into a `String` be brittle (Rust)?	The number is only meaningful for that specific string state; the string can change and the index becomes invalid.
What problem do string slices solve compared to returning indices (Rust)?	They tie the “substring” result to the underlying data via borrowing, so invalid states are rejected by the compiler.
What is a string slice type in Rust?	`&str`.
How do you create a string slice from a `String` (Rust)?	With range syntax: `&s[start..end]` (e.g., `&s[..2]`, `&s[3..]`, `&s[..]`).
In Rust slice ranges like `a..b`, is `b` inclusive or exclusive?	Exclusive: `a..b` includes `a` up to but not including `b`.
What does `&s[..]` mean for a `String` in Rust?	A slice of the entire string.
What constraint applies to string-slice indices in Rust (`&s[a..b]`)?	They must be at valid UTF-8 character boundaries; slicing in the middle of a multibyte char will panic.
Why is `fn f(s: &str)` more flexible than `fn f(s: &String)` (Rust)?	`&str` works with both string literals and `String` values (via deref coercions / slicing).
What is the type of a string literal in Rust (e.g., `"Hello"`)?	`&'static str` (a string slice pointing into the binary).
Why does Rust reject mutating a `String` while a slice into it is still used?	Borrowing rules: you can’t take a mutable borrow if an immutable borrow is still active and later used.
What is an array/slice slice type in Rust (general case)?	`&[T]` (e.g., slicing an array gives `&[i32]`).
Why use a struct instead of a tuple (Rust)?	Fields are named, so meaning doesn’t depend on position and code is clearer/flexible.
How do you define a struct type in Rust?	With `struct Name { field: Type, ... }`.
How do you create a struct instance in Rust?	`Name { field1: value1, field2: value2, ... }`.
To mutate a struct field, what must be true about the binding (Rust)?	The entire instance binding must be mutable (e.g., `let mut user = ...;`).
Can you make only one field mutable in an otherwise immutable struct value (Rust)?	No; Rust requires the whole instance to be mutable to mutate any field.
What is the field init shorthand in Rust?	When a variable name matches a field name, you can write `field` instead of `field: field`.
What does struct update syntax `..other` do (Rust)?	Fills in unspecified fields from another instance of the same struct type.
Why can struct update syntax make the original instance unusable (Rust)?	It moves non-`Copy` fields (like `String`) from the source into the new value.
When does struct update syntax copy instead of move fields (Rust)?	When the field type implements `Copy` (e.g., `bool`, integers).
What is a tuple struct in Rust?	A struct type with unnamed fields, declared like `struct Name(T1, T2, ...)`.
Why use a tuple struct (Rust)?	To give a tuple-like value a distinct type name without naming each field.
How do you destructure a tuple struct (Rust)?	Use the type name in the pattern, e.g. `let Point(x, y, z) = origin;`.
Why might a struct store `String` instead of `&str` (Rust)?	To own its data so it stays valid as long as the struct value exists.
What’s required to store references in a struct (Rust)?	Lifetime parameters on the struct and reference fields (e.g., `struct User<'a> { name: &'a str }`).
Can you move a non-`Copy` field out of `&Struct` (Rust)?	No; you can only borrow fields through a shared reference (moving out would be invalid).
In `println!`, what does `{}` require from the value’s type (Rust)?	An implementation of `std::fmt::Display`.
What is `std::fmt::Debug` intended for (Rust)?	Developer/debugging output (inspect internal state).
How do you print a value using Debug formatting in `println!` (Rust)?	Use `{:?}`, e.g. ``println!("{value:?}")``.
How do you pretty-print Debug output in `println!` (Rust)?	Use `{:#?}`, e.g. ``println!("{value:#?}")``.
How do you add a default Debug implementation for a struct (Rust)?	Add `#[derive(Debug)]` above the struct definition.
What does `dbg!(expr)` do (Rust)?	Prints file/line + the expression and its value (using `Debug`), and returns the expression’s value.
Does `dbg!` print to stdout or stderr (Rust)?	Stderr.
Why can `dbg!(value)` affect ownership (Rust)?	`dbg!` takes ownership of the expression result (so it can move non-`Copy` values).
How do you use `dbg!` without moving a value (Rust)?	Debug-print a reference: `dbg!(&value)`.
How do you attach behavior like `area` to a struct as a method (Rust)?	Use an `impl` block, e.g. ``impl Rectangle { fn area(&self) -> u32 { ... } }``.
What distinguishes a method from a free function (Rust)?	A method is defined in a type context (e.g., `impl`) and has a `self` receiver as its first parameter.
Where do you define methods for a struct (Rust)?	Inside an `impl Type { ... }` block.
What is `&self` shorthand for in a method signature (Rust)?	`self: &Self`.
What does `Self` mean inside an `impl Type` block (Rust)?	An alias for the type being implemented (`Type`).
What are the three common receiver forms for methods (Rust)?	`&self` (borrow immutably); `&mut self` (borrow mutably); `self` (take ownership).
Why is `&self` a common choice for many methods (Rust)?	It lets the method read data without taking ownership or mutating the receiver.
When is a method with receiver `self` (takes ownership) typically used (Rust)?	When the method consumes/transforms the value and you want to prevent use of the original afterward.
Why doesn’t Rust need a `->` operator for method calls?	Method calls use dot syntax plus automatic referencing/dereferencing for the receiver.
What is “automatic referencing/dereferencing” in Rust method calls?	The compiler inserts `&`, `&mut`, or `*` so the receiver matches the method’s `self` type.
How do you define and call a constructor-like associated function (Rust)?	Define a function without `self` in an `impl` returning `Self` (often named `new`, but not special) and call it as `Type::function(...)` (e.g., `fn square(size: u32) -> Self { Self { ... } }`).
What is an enum in Rust?	A type defined by enumerating its possible variants (a value is exactly one variant at a time).
How do you refer to/construct a variant value (Rust)?	`EnumName::Variant` (optionally with payload arguments).
Why do enum variants use `Enum::Variant` (Rust)?	Variants are namespaced under the enum type, avoiding name collisions.
Why are enums a better fit than structs for “one of several cases” (Rust)?	An enum enforces “exactly one of these variants” while keeping a single type for all cases.
How do you attach data to an enum variant (Rust)?	Put data in the variant definition, e.g. `V4(String)` or `Move { x: i32, y: i32 }`.
What are the three common enum-variant shapes (Rust)?	Unit-like (no data), tuple-like (unnamed fields), and struct-like (named fields).
Can different enum variants carry different types/amounts of data (Rust)?	Yes; each variant can define its own payload.
In Rust, why is an enum variant like `IpAddr::V4(...)` described as a constructor?	Each tuple/struct-like variant is a value constructor that builds the enum value.
Can you define methods on an enum (Rust)?	Yes; use `impl EnumName { fn ... }`.
What is `Option<T>` in Rust?	An enum representing an optional value: `Some(T)` or `None`.
Does Rust have `null`?	No; absence is represented explicitly with `Option<T>`.
Why is `Option<T>` safer than nulls (Rust)?	`Option<T>` is a distinct type, so the compiler forces you to handle the “no value” case.
What does the `T` in `Option<T>` mean (Rust)?	A generic type parameter: the type inside `Some(T)`.
Are `Option<i32>` and `Option<char>` the same type (Rust)?	No; substituting a different `T` produces a different concrete type.
Why does `let x = None;` often need a type annotation (Rust)?	`None` alone doesn’t provide enough information to infer `T`.
How do you type-annotate a `None` value (Rust)?	`let x: Option<i32> = None;` (or another concrete `Option<T>`).
Why can’t you use an `Option<T>` as if it were a `T` (Rust)?	They’re different types; you must handle `Some` vs `None` first.
What Rust construct is commonly used to handle different enum variants?	`match`.
What does `match` on an enum enable (Rust)?	Branching by variant and (optionally) binding variant payloads in patterns.
In a `match`, what does a pattern like `Some(v)` do (Rust)?	Matches the `Some` variant and binds the inner value to `v`.
In a `match` on `Option<T>`, how do you handle the “no value” case (Rust)?	Add a `None => ...` arm.
What is `if let` used for (Rust)?	A concise way to run code when one pattern matches (common with enums).
Example: how do you run code only when an `Option` is `Some` (Rust)?	`if let Some(v) = opt { ... }`.
When do you need `{ ... }` in a match arm (Rust)?	When the arm runs multiple statements/lines; braces wrap a block expression.
In a `match`, what becomes the value of the entire `match` expression (Rust)?	The value of the expression/block in the matching arm.
Pattern binding: what does `Coin::Quarter(state)` do in a `match` (Rust)?	Matches `Quarter(...)` and binds the inner `UsState` value to `state`.
In `match dice_roll { 3 => ..., 7 => ..., other => ... }`, what does `other` do (Rust)?	It’s a catch-all pattern that binds the unmatched value for use in that arm.
What’s the difference between `other => ...` and `_ => ...` in `match` (Rust)?	`other` matches and binds the value; `_` matches but does not bind (ignores it).
Why must a catch-all arm go last in a `match` (Rust)?	Because it matches anything; later arms would be unreachable (Rust warns/errors about unreachable patterns).
What does `_ => ()` mean in a `match` arm (Rust)?	Match all remaining cases and “do nothing” (evaluate to unit `()`).
What compiler error do you get for a non-exhaustive `match` (Rust)?	A compile-time error for “non-exhaustive patterns” (e.g., `None` not covered).
Why does Rust require `match` to be exhaustive (Rust)?	So every possible value is handled (or explicitly ignored with `_`), preventing missed cases.
What is a crate in Rust?	The smallest compilation unit Rust considers; a crate is a tree of modules compiled together.
What is a binary crate (Rust)?	A crate that compiles to an executable and must define `fn main()`.
What is a library crate (Rust)?	A crate that provides reusable functionality; it has no `main` and doesn’t compile to an executable by itself.
What is the crate root (Rust)?	The source file Rust starts from; it defines the root module of the crate.
What is a package in Rust/Cargo?	A bundle of one or more crates described by a `Cargo.toml`.
In a Cargo package, what file defines a library crate by convention?	`src/lib.rs`.
In a Cargo package, what file defines the default binary crate by convention?	`src/main.rs`.
How do you add multiple binary crates to one package (Cargo convention)?	Add files under `src/bin/`; each file is a separate binary crate.
Where does Rust look for a module declared with `mod garden;`?	Inline in the current file, in `src/garden.rs`, or in `src/garden/mod.rs`.
Where does Rust look for a submodule declared with `mod vegetables;` inside `garden`?	Inline in the parent module, in `src/garden/vegetables.rs`, or in `src/garden/vegetables/mod.rs`.
How do you refer to an item inside a module (Rust)?	Use its path, such as `crate::garden::vegetables::Asparagus`.
What is the default privacy rule for items in a module (Rust)?	Items are private to their parent modules unless marked `pub`.
Why might you need `pub` on items inside a `pub mod` (Rust)?	A public module does not make its contents public automatically; each item is still private unless marked `pub`.
What does `use` do within a scope (Rust module system)?	It creates a local shortcut to a path so you can refer to the item with a shorter name.
What is an absolute path in Rust modules?	A path that starts at the crate root; for the current crate it begins with `crate`, and for external crates it begins with the crate name.
What is a relative path in Rust modules?	A path that starts from the current module and uses `self`, `super`, or a name in the current module.
What does `super` mean at the start of a Rust path?	It refers to the parent module of the current module.
What is the idiomatic `use` style for functions in Rust?	Import the parent module and qualify the function call with that module.
What is the idiomatic `use` style for structs, enums, and other items in Rust?	Import the full path to the item itself.
What does `as` do in a Rust `use` statement?	It gives an imported item a different local name.
Why is re-exporting with `pub use` useful in Rust?	It lets you expose a public API that differs from your internal module layout.
How do you use items from an external crate in Rust?	Add the crate to `Cargo.toml`, then use paths starting with the crate name.
What are nested paths in a Rust `use` statement for?	They let you bring multiple items with the same path prefix into scope in one statement.
What does a glob import like `use path::*;` do in Rust?	It brings all public items from that path into the current scope.
If a Rust struct is `pub`, are its fields automatically public?	No; each field is still private unless it is individually marked `pub`.
If a Rust enum is `pub`, are its variants public?	Yes; making the enum public also makes its variants public.
What does `mod name;` mean when a Rust module body is moved to another file?	It declares the module in the tree and tells the compiler to load that module's body from the corresponding file.
Where should the file for a child module live in Rust?	Under a directory named for its parent module path, so the file layout mirrors the module tree.
Why would a child module like `hosting` live in `src/front_of_house/hosting.rs` instead of `src/hosting.rs`?	Because it is a child of `front_of_house`, not a crate-root module, so its file path follows the parent module path.
Why is `mod` not like a textual `include` in Rust?	`mod` declares a module once in the module tree; other code refers to that module by path instead of reloading the file.
After a Rust module has been declared with `mod`, how should other files refer to its items?	By using the module path where it was declared, not by repeating the `mod` declaration.
Does moving Rust module code into another file change the module tree or item paths?	No; it only changes where the code is stored, not the module's place in the tree.
What are the two supported file-path styles for an out-of-line Rust module?	The direct-file style like `src/foo.rs` or `src/foo/bar.rs`, and the older `mod.rs` style like `src/foo/mod.rs` or `src/foo/bar/mod.rs`.
What are collections in Rust?	Data structures that can hold multiple values and whose data is typically stored on the heap.
What is `Vec<T>` in Rust?	A growable, heap-allocated collection of values of one type stored next to each other in memory.
When might `Vec::new()` need a type annotation (Rust)?	When the vector is empty and Rust has not yet seen any elements that reveal the element type.
What does the `vec!` macro do in Rust?	It creates a vector initialized with the values you provide.
What does `push` do on a Rust vector?	It appends a new element to the end of the vector.
What is the difference between indexing and `get` on a vector (Rust)?	Indexing returns a reference and panics if out of bounds; `get` returns an `Option` instead.
Why might `v.get(i)` be preferable to `v[i]` in Rust?	It lets you handle missing elements explicitly instead of panicking.
Why can’t you push to a vector while holding a reference to one of its elements (Rust)?	Growing the vector may reallocate its storage and invalidate existing references, so the borrow checker rejects that pattern.
What does iterating with `for x in &v` yield for a Rust vector?	Immutable references to the vector's elements.
What does iterating with `for x in &mut v` yield for a Rust vector?	Mutable references to the vector's elements.
Why do you need `*x` to change an element while iterating with `&mut v` (Rust)?	Because the loop variable is a mutable reference, so you must dereference it to modify the underlying value.
How can Rust store multiple logical value kinds in one vector?	By using a vector of an enum whose variants represent the allowed kinds.
What happens to a vector's elements when the vector is dropped in Rust?	The vector's contents are dropped too.
What is the difference between `String` and `&str` in Rust?	`String` is an owned, growable UTF-8 string; `&str` is a borrowed string slice.
What does `String::from(...)` do in Rust?	It creates an owned `String` from string data such as a string literal.
What is the difference between `push_str` and `push` on a Rust `String`?	`push_str` appends a string slice, while `push` appends a single `char`.
What happens to the left operand in `s1 + &s2` (Rust)?	The left `String` is moved and reused to build the result.
Why can `s1 + &s2` accept `&String` on the right when `add` expects `&str` (Rust)?	Rust applies deref coercion from `&String` to `&str`.
Why is `format!` often preferable to repeated `+` for strings in Rust?	It is easier to read and does not take ownership of its arguments.
Why doesn’t Rust allow string indexing like `s[0]`?	Strings are UTF-8, so byte positions do not reliably match characters, and finding the nth character cannot guarantee O(1) access.
How can the same Rust string be viewed in multiple ways?	As bytes, as Unicode scalar values (`char`s), or as grapheme clusters.
What does a string slice like `&s[a..b]` mean in Rust?	A borrowed `&str` containing the bytes in that range, as long as the bounds are on valid UTF-8 character boundaries.
What happens if you slice a Rust string at a non-character boundary?	The program panics at runtime.
When should you use `.chars()` on a Rust string?	When you want to iterate over Unicode scalar values.
When should you use `.bytes()` on a Rust string?	When you want to iterate over the raw UTF-8 bytes.
Why doesn't Rust's standard library provide grapheme-cluster iteration directly?	Grapheme clustering is more complex than byte or scalar-value iteration and is left to external crates.
What is `HashMap<K, V>` in Rust?	A hash map that stores key-value pairs using a hashing function.
What does `insert` do on a Rust `HashMap`?	It inserts a key-value pair, replacing the old value if the key already exists.
What does `get` return on a Rust `HashMap`?	An `Option<&V>` for the requested key.
What should you remember about iterating over a Rust `HashMap`?	Key-value pairs are visited in arbitrary order.
What happens when you insert owned `String` keys or values into a Rust `HashMap`?	They are moved into the hash map, which becomes their owner.
What does `entry(key).or_insert(value)` do on a Rust `HashMap`?	It inserts the value only if the key is absent and returns a mutable reference to the entry's value.
How can `entry(...).or_insert(...)` support updates based on an old value in Rust?	It returns a mutable reference to the stored value, which you can modify in place.
What is the default hashing tradeoff for Rust's `HashMap`?	It uses SipHash for better resistance to hash-table DoS attacks at some performance cost.
What is `panic!` used for in Rust?	To stop execution when the program reaches an unrecoverable error.
What is the difference between unwinding and aborting on panic in Rust?	Unwinding cleans up stack frames as it exits; aborting ends the program immediately without cleanup.
Why might a Rust project choose `panic = 'abort'`?	To reduce binary size and skip the runtime cost of unwinding.
Why does out-of-bounds indexing panic in Rust?	Because there is no valid element to return, and continuing would be unsafe.
What is a backtrace used for when debugging a Rust panic?	To show the call stack that led to the panic so you can trace the source of the problem.
How should you read a Rust panic backtrace at a high level?	Start from the top and look for the first frame in code you wrote; that is usually where the bug originates.
Why is `Result<T, E>` used in Rust?	To represent operations that can succeed with `Ok(T)` or fail with `Err(E)` in a recoverable way.
Why is matching on `Result` useful in Rust?	It lets you branch explicitly on success versus failure and handle each case differently.
Why might code inspect `ErrorKind` when handling an I/O error in Rust?	To react differently to specific failure causes, such as creating a file when it is missing.
What does `unwrap` do on a `Result` in Rust?	It returns the `Ok` value or panics if the result is `Err`.
Why is `expect` often preferable to `unwrap` in Rust?	It lets you provide context about why failure was considered impossible, making panics easier to debug.
What does it mean to propagate an error in Rust?	Return the error to the caller instead of handling it in the current function.
What does the `?` operator do on a `Result` in Rust?	It returns the inner `Ok` value or propagates the `Err` early from the function.
What extra behavior does `?` have for error types in Rust?	It uses `From` conversion so the error can be transformed into the function's return error type.
Why can the `?` operator make Rust code more readable?	It removes repetitive match-and-return boilerplate for common propagation patterns.
What does the `?` operator do on an `Option` in Rust?	It returns the inner `Some` value or returns `None` early from the function.
Why can’t you freely use `?` in any Rust function?	The function’s return type must be compatible with the residual type that `?` may return early.
Why can `main` return `Result<(), E>` in Rust?	So it can use `?` and let errors end the program through the standard termination mechanism.
When is returning `Result` usually the better default than panicking in Rust?	When failure is an expected possibility and the caller may reasonably recover.
Why are `unwrap` and `expect` often acceptable in examples, prototypes, and tests?	They keep the code concise when robust error handling is not the main point or when failure should fail fast.
When is `expect` reasonable because the programmer knows more than the compiler?	When logic outside the type system guarantees success in a specific case, such as parsing a hardcoded valid value.
When is panicking appropriate because of a bad state in Rust?	When an invariant, contract, or assumption has been violated and continuing would be incorrect, unsafe, or meaningless.
What is a custom validation type in Rust?	A type whose constructor enforces invariants before values of that type can exist.
Why can a custom validated type reduce repeated runtime checks in Rust?	Functions can rely on the type’s invariant instead of revalidating the raw value everywhere.
Why should a validation type keep its fields private in Rust?	So callers must construct it through checked APIs that preserve the invariant.
Why are generics useful in Rust?	They let one definition work across multiple concrete types, reducing duplication without giving up compile-time checking.
What does a generic function signature like `fn f<T>(x: T) -> T` mean?	The same function body can be compiled for different concrete types substituted for `T`.
Why does comparing values of a generic type with `>` require a trait bound in Rust?	Because not every type supports ordering, so the type must implement `PartialOrd`.
What does a trait bound do on a generic type in Rust?	It restricts the generic type to only those types that implement the required behavior.
What does `struct Point<T> { x: T, y: T }` imply about `x` and `y`?	They must have the same concrete type for a given `Point<T>` instance.
Why would a Rust struct use multiple generic parameters like `Point<T, U>`?	To allow different fields to use different concrete types.
Why can methods be implemented on `impl<T> Point<T>` in Rust?	Because the methods are intended to work for every concrete `Point<T>` regardless of what `T` is.
Why would Rust use `impl Point<f32>` instead of `impl<T> Point<T>`?	To define methods that only make sense for one specific concrete type.
Can a Rust method introduce its own generic parameters separate from the struct's generics?	Yes; method-level generics can be declared after the method name and are independent of the type's own generic parameters.
What is monomorphization in Rust?	The compile-time process of generating concrete versions of generic code for each used concrete type.
Do Rust generics add runtime dispatch overhead by default?	No; monomorphization produces type-specific code at compile time, so generic code runs like handwritten concrete code.
What does a trait define in Rust?	A shared set of method signatures that describe behavior types can implement.
How is a trait implemented for a type in Rust?	With `impl TraitName for Type { ... }`, providing the required method bodies.
What does the orphan rule prevent in Rust?	Implementing an external trait for an external type when neither is local to your crate.
Why does Rust's orphan rule matter?	It prevents conflicting trait implementations across crates and preserves coherence.
What is a default trait implementation in Rust?	A method body provided in the trait definition that implementors may inherit or override.
Can a default trait method call another trait method in Rust?	Yes; a default method can build behavior from another required or default trait method.
What does `&impl Summary` mean in a Rust function parameter?	The parameter can be any type that implements the `Summary` trait.
What is the difference between `fn f(a: &impl Trait, b: &impl Trait)` and `fn f<T: Trait>(a: &T, b: &T)`?	The first allows different concrete types for `a` and `b`; the second requires the same concrete type for both.
What does `T: Summary + Display` mean in Rust?	The generic type `T` must implement both `Summary` and `Display`.
Why use a `where` clause for trait bounds in Rust?	To move complex bounds out of the parameter list and make the signature easier to read.
What does `-> impl Summary` mean in a Rust return type?	The function returns some single concrete type that implements `Summary` without naming that type in the signature.
What is a key limitation of `impl Trait` in return position in Rust?	All return paths must resolve to the same concrete type.
What is a blanket implementation in Rust?	An implementation of a trait for every type that satisfies some trait bound.
What is a lifetime in Rust?	The span of code for which a reference is valid.
What is the main purpose of lifetimes in Rust?	To prevent dangling references by ensuring borrowed data outlives the references to it.
What does `fn longest<'a>(x: &'a str, y: &'a str) -> &'a str` mean in Rust?	The returned reference must be valid for the lifetime shared by both input references, effectively the shorter overlapping lifetime.
When returning a reference from a Rust function, what must its lifetime be tied to?	One of the input references, unless ownership of a new value is returned instead.
Why can't a Rust function return a reference to a local variable it created?	The local value is dropped when the function ends, so the returned reference would dangle.
When does a Rust struct need a lifetime parameter?	When it stores references in its fields.
What are Rust's three lifetime elision rules for functions and methods?	Each input reference gets its own lifetime; if there is exactly one input lifetime, it is assigned to all output references; if a method has `&self` or `&mut self`, that lifetime is assigned to output references.
What does the `'static` lifetime mean in Rust?	The reference can remain valid for the entire duration of the program.
What does `cargo test` do in Rust?	It builds the project in test mode and runs the resulting test binaries.
When is `assert_eq!` preferable to `assert!` in Rust?	When you want to check equality and see both compared values if the assertion fails.
When is `assert_ne!` useful in Rust tests?	When you only need to assert that two values are different.
What traits must values implement to work with `assert_eq!` or `assert_ne!` in Rust?	`PartialEq` for comparison and `Debug` so failed assertions can print the values.
Why add a custom message to `assert!`, `assert_eq!`, or `assert_ne!` in Rust tests?	To make failures easier to diagnose by explaining what the assertion was supposed to guarantee.
What does `#[should_panic]` mean on a Rust test?	The test passes only if the code in the test panics.
Why can `#[should_panic]` be too imprecise in Rust tests?	It accepts any panic, even if the panic happened for the wrong reason.
What does `#[should_panic(expected = \"...\")]` add to a Rust test?	It requires the panic message to contain a specific substring, making the test more precise.
Why might a Rust test return `Result<(), E>` instead of using only assertion macros?	It lets the test use `?` and fail by returning `Err` instead of panicking directly.
Why can't a Rust test use both `#[should_panic]` and a `Result` return type?	`#[should_panic]` expects the test to panic, while a `Result`-based test signals failure by returning `Err`.
How do Rust tests run by default when there are multiple tests?	They run in parallel on multiple threads.
Why must Rust tests avoid shared mutable external state by default?	Parallel test execution can make tests interfere with each other and produce nondeterministic failures.
How can you force Rust tests to run one at a time?	Run `cargo test -- --test-threads=1`.
What output does Rust capture by default for passing tests?	Standard output is captured and hidden for tests that pass.
How can you show `println!` output from passing Rust tests?	Run `cargo test -- --show-output`.
How can you run a single Rust test by name?	Pass the test function name to `cargo test`.
How can you run multiple Rust tests that share part of a name?	Pass a substring to `cargo test`, which runs tests whose names match it.
What does `#[ignore]` do on a Rust test?	It marks the test so it is skipped during a normal `cargo test` run.
What is the difference between `cargo test -- --ignored` and `cargo test -- --include-ignored` in Rust?	`--ignored` runs only ignored tests; `--include-ignored` runs both ignored and non-ignored tests.
What is the difference between unit tests and integration tests in Rust?	Unit tests exercise code in isolation and may test private details; integration tests use only the public API from outside the crate.
Why is `#[cfg(test)]` used on a Rust unit-test module?	So the test code is compiled only when running tests, not during a normal build.
Can Rust unit tests call private functions from the same source file?	Yes; the test module is a child module and can access items from its parent module.
Where do Rust integration tests live?	In top-level files under the `tests/` directory.
Why must a Rust integration test import the library crate explicitly with `use`?	Each file in `tests/` is compiled as a separate crate.
Why don't integration-test files in Rust need `#[cfg(test)]`?	Cargo compiles files in `tests/` only when running tests.
How can you run only one Rust integration test file?	Run `cargo test --test <file_stem>`.
Why is `tests/common.rs` a poor place for shared Rust integration-test helpers?	Cargo treats it as its own integration test crate and it shows up as a separate test target.
How should shared helper code be organized for Rust integration tests?	Put it in a submodule such as `tests/common/mod.rs` and import it with `mod common;`.
Why can't a pure binary Rust crate be integration-tested through the `tests/` directory?	Other crates cannot `use` items from `src/main.rs`; only library crates expose reusable public APIs.
Why do Rust projects often keep logic in `src/lib.rs` even when they provide a binary?	It makes the core functionality available for integration testing and leaves `src/main.rs` as a thin entry point.
What does `std::env::args()` provide in Rust?	An iterator over the command-line arguments passed to the program.
Why does `collect()` often need a type annotation when collecting command-line arguments in Rust?	Because Rust must know which collection type to build, such as `Vec<String>`.
When should Rust code use `std::env::args_os()` instead of `std::env::args()`?	When the program must accept command-line arguments that may contain invalid Unicode.
Why is it useful to group command-line settings into a `Config` struct in Rust?	It makes the program's configuration explicit and keeps related values together.
Why is cloning argument strings into a `Config` struct often acceptable in a small Rust CLI?	It simplifies ownership and lifetime management for small values where the extra copy cost is negligible.
When should a Rust binary split code between `main.rs` and `lib.rs`?	When the program logic starts growing, so `main` can stay focused on setup while reusable logic moves into the library.
What responsibilities should usually remain in `main` for a Rust CLI?	Parse arguments, set up configuration, call the main `run` logic, and report any top-level errors.
Why is returning `Result` from `Config::build` better than panicking on missing CLI arguments?	It lets the program produce a user-facing usage error instead of an internal panic-style failure.
What does `unwrap_or_else` enable on a `Result` in Rust CLI setup code?	Custom handling of the error case while still extracting the success value when present.
Why is `process::exit(1)` used in a Rust CLI after a top-level error?	To terminate the program with a nonzero exit status that signals failure to the calling process.
Why is it useful for a Rust CLI's `run` function to return `Result<(), Box<dyn Error>>`?	It lets `run` propagate different error types with `?` while signaling that success has no meaningful return value.
What does `Box<dyn Error>` mean in a Rust function return type?	The function may return any heap-allocated error type that implements the `Error` trait.
Why is the `?` operator useful in a Rust CLI `run` function?	It propagates errors concisely instead of requiring explicit `match` handling at each step.
Why does a search function like `fn search<'a>(query: &str, contents: &'a str) -> Vec<&'a str>` tie the output lifetime to `contents`?	Because the returned string slices must point into the searched text, not into the query string.
Why is test-driven development helpful when implementing a function like `search`?	It forces the intended behavior to be specified first and lets the implementation grow just enough to satisfy the tests.
How can a Rust program implement case-insensitive search simply?	Lowercase the query and each candidate line before checking whether the line contains the query.
What is the tradeoff of using `to_lowercase()` for case-insensitive search in Rust?	It is simple and practical, but not a fully general Unicode-aware case-folding solution.
How can a Rust CLI enable optional behavior through an environment variable?	Read it with `env::var` and treat its presence or value as configuration.
Why should error messages in a CLI be written to `stderr` instead of `stdout`?	So users can redirect normal output to a file without losing visibility into errors.
What is the difference between `println!` and `eprintln!` in Rust?	`println!` writes to standard output, while `eprintln!` writes to standard error.
