What is the Python Data Model?	The set of protocols (special methods) that let objects support core language features and idiomatic syntax.
What are “special methods” in Python?	Methods with `__double_underscores__` that the interpreter calls to implement language features.
Why prefer built-ins over calling special methods directly?	Built-ins can add behavior and may be faster (especially for built-in types).
How does `for x in obj:` start iteration?	It calls `iter(obj)`, which uses `obj.__iter__()` or may fall back to `obj.__getitem__()`.
What two special methods are enough to make an object behave like a basic sequence?	`__len__` and `__getitem__`.
Why implement standard protocols in your class?	So standard library functions work with it directly, without adapters.
How does `x in collection` work if `__contains__` is missing?	It can do a sequential scan using iteration.
If you implement only one, prefer `__repr__` or `__str__`?	`__repr__`.
What’s a good rule of thumb for `__repr__`?	Make it unambiguous; if possible, make it resemble code to recreate the object.
How do you force `repr` formatting in an f-string?	Use `!r`, e.g. ``f"{x!r}"``.
What’s the expected behavior of infix operators for user-defined types?	They usually return a new object and don’t mutate the operands.
What special method controls truthiness?	`__bool__`
If `__bool__` isn’t defined, what does Python use for truthiness?	`__len__` (zero → falsy; nonzero → truthy).
If neither `__bool__` nor `__len__` is defined, are instances truthy?	Yes; instances are truthy by default.
Why is `len(x)` not `x.len()` in Python (design rationale)?	`len` is a language-level operation optimized for built-ins, and user types participate via `__len__`.
What is a container sequence (Python)?	A sequence that holds references to contained objects (can be heterogeneous and nested).
What is a flat sequence (Python)?	A sequence that stores simple values in its own memory (typically one primitive machine type).
Why are flat sequences usually more memory-efficient than container sequences?	They store elements as packed values in contiguous memory, which avoids creating many separate Python objects with their own headers and references.
Give examples of mutable sequences (Python).	`list`; `bytearray`; `array.array`; `collections.deque`.
Give examples of immutable sequences (Python).	`tuple`; `str`; `bytes`.
Why can a list comprehension be clearer than a for-loop when building a list?	Its intent is explicit: it always builds a new list (a for-loop can do many tasks).
Listcomps vs `map`/`filter`: why often prefer list comprehensions (Python)?	They’re usually more readable and avoid awkward `lambda` usage while doing the same job.
Do list comprehension loop variables leak in Python 3?	No; comprehension variables have their own local scope (they don’t clobber outer names).
How do you build a Cartesian product with a list comprehension?	Use multiple `for` clauses, e.g. `[(c, s) for c in colors for s in sizes]`.
Why are tuples useful as records (Python)?	Field order/length is fixed; unpacking avoids error-prone indexing.
Why can tuples be smaller/faster than lists (high level)?	Fixed-size allocation and less overhead enable optimizations compared to growable lists.
Why can a tuple’s value still change even though tuples are “immutable”?	If it contains references to mutable objects, those objects can mutate.
When is a tuple hashable, and how can you quickly check?	Only if all items are hashable; `hash(t)` raises `TypeError` if it contains mutable/unhashable items.
How do you swap two variables idiomatically (Python)?	`a, b = b, a`.
How do you pass a tuple as separate positional arguments (Python)?	Use `*t` in the call, e.g. `divmod(*t)`.
In unpacking assignment, what does `*rest` do?	It captures any remaining items into a list.
What is nested unpacking (Python)?	Unpacking nested structures by matching the nesting shape on the left-hand side.
How can unpacking assert an iterable has exactly one item (Python)?	Use a 1-item target like `[record] = iterable` (it raises if not exactly one).
What is structural pattern matching used for (Python ≥ 3.10)?	Matching data shapes and destructuring (binding parts) with `match`/`case`.
When does a sequence pattern match in `match`/`case` (Python)?	The subject is a sequence; pattern and subject lengths match; corresponding items (including nested) match.
In a sequence pattern, what does `*rest` (or `*_`) do (Python)?	`*rest` matches remaining items and binds them as a list; `*_` matches and ignores them.
What is a guard in `match`/`case` (Python)?	An `if` condition that is evaluated only after the pattern matches.
Why do Python slices/ranges exclude the stop index?	It makes lengths easy (`stop-start`) and allows clean splits like `s[:x]` and `s[x:]` without overlap.
What does the slice step/stride do (Python)?	`seq[a:b:c]` skips by `c`; negative steps iterate in reverse.
What does `seq[start:stop:step]` call under the hood (Python)?	`seq.__getitem__(slice(start, stop, step))`.
How can naming `slice()` objects improve readability (Python)?	It lets you label fixed-width fields/ranges instead of scattering hardcoded indices.
Why is `[[...]] * n` (or `[['_']*m] * n`) a common bug pattern (Python)?	It repeats references to the same inner list, so one mutation affects multiple “rows”.
What’s the safe way to build a list of independent inner lists (Python)?	Use a list comprehension, e.g. `[['_']*m for _ in range(n)]`.
What determines whether `a += b` mutates in place (Python)?	If `__iadd__` exists it’s used (often in-place for mutables); otherwise Python falls back to `a = a + b` (new object).
Why can `t[2] += [...]` be surprising when `t` is a tuple containing a list?	It may mutate the inner list, then raise `TypeError` when trying to store back into the tuple (augmented assignment isn’t atomic).
`list.sort()` vs `sorted()` (Python): what’s the difference?	`list.sort()` sorts in place and returns `None`; `sorted()` returns a new list from any iterable.
What do `key=` and `reverse=` do in sorting (Python), and what does “stable” mean?	`key` computes sort keys; `reverse=True` reverses order; stable means equal keys keep their original relative order.
When is `array.array` a better choice than `list` (Python)?	When you need large homogeneous numeric data and want lower memory overhead.
What does an `array` typecode specify (Python)?	The underlying C storage type for items (and it restricts what values you can store).
What is `memoryview` used for (Python)?	To share/slice binary or numeric data without copying bytes (a shared-memory view).
Why use `collections.deque` for a FIFO queue instead of a list?	`deque` supports fast append/pop from both ends; `list.pop(0)` is costly because it shifts elements.
What happens when you append to a bounded `deque` that is full (Python)?	It discards an item from the opposite end to make room.
Why are dicts so fundamental in Python (implementation)?	Class/instance attributes, module namespaces, and function keyword arguments are represented with dictionaries in memory.
What data structure underlies Python `dict` (high level)?	A hash table.
Which built-in types are also based on hash tables (Python)?	`set` and `frozenset` (and `dict` itself).
In `d1 | d2`, which values win if keys overlap (Python)?	The right operand’s values (from `d2`) overwrite.
Pattern matching: what can mapping patterns match (Python ≥ 3.10)?	Instances of actual or virtual subclasses of `collections.abc.Mapping`.
Do mapping patterns require an exact match of all keys (Python match/case)?	No; mapping patterns succeed on partial matches (extra keys in the subject are allowed).
How can you capture extra key/value pairs in a mapping pattern (Python match/case)?	Use `**rest` to bind remaining items as a dict (`**rest` must be last).
What are `Mapping` and `MutableMapping` in Python?	ABCs in `collections.abc` that formalize mapping interfaces (useful for `isinstance` checks).
Why prefer `isinstance(x, abc.Mapping)` over `type(x) is dict` (Python)?	It supports alternative mapping types, not just the concrete `dict`.
What does it mean for a key to be “hashable” (Python)?	It has a hash code that doesn’t change during its lifetime and supports equality comparison (`__hash__` + `__eq__`).
What must be true of equal hashable objects (Python)?	If `a == b`, then `hash(a) == hash(b)`.
Which common types are hashable by default (Python examples)?	Numbers, `str`, and `bytes` (flat immutable types).
When is a container type hashable (Python)?	If it’s immutable and all contained items are hashable (e.g., certain tuples).
Why is `frozenset` always hashable (Python)?	Because it can contain only hashable elements.
Why can a custom `__eq__` make hashability tricky (Python)?	To remain hashable, `__hash__` must stay consistent with `__eq__` and depend only on immutable state.
Why can `dict.get` be suboptimal when updating a mutable value (Python)?	You may do a lookup, mutate, then do another lookup/store back—extra work and easy to write awkwardly.
What does `dict.setdefault(k, default)` do (Python)?	If `k` exists, returns `d[k]`; otherwise sets `d[k] = default` and returns it.
Why is `d.setdefault(k, []).append(x)` idiomatic (Python)?	It ensures a list exists at `k` with a single lookup, then appends.
What is `collections.defaultdict` (Python)?	A dict-like mapping that creates a default value on missing `d[k]` lookups using a `default_factory`.
What does `defaultdict.default_factory` do (Python)?	It’s a callable used to create and insert a default value when `__getitem__` sees a missing key.
When does a `defaultdict` call `default_factory` (Python)?	Only on `d[k]` lookups via `__getitem__`, not on `d.get(k)` or `k in d`.
What is the `__missing__` method for (Python mappings)?	A hook a `dict` subclass can define; `dict.__getitem__` calls it when a key is missing.
Does `k in d` trigger `__missing__` in a `dict` subclass (Python)?	No; membership tests don’t fall back to `__missing__` unless you implement that logic yourself.
Why must a `__missing__` implementation often special-case string keys (Python example)?	To avoid infinite recursion when `__missing__` retries lookup using a transformed key.
Why is subclassing `collections.UserDict` often easier than subclassing `dict` (Python)?	UserDict uses composition (`self.data`), avoiding some built-in shortcuts that can make subclassing `dict` tricky.
Is a `mappingproxy` (from `MappingProxyType`) a snapshot (Python)?	No; it’s dynamic—changes to the original mapping are reflected in the proxy.
What do `dict.keys()`, `dict.items()`, and `dict.values()` return (Python 3)?	View objects (`dict_keys`, `dict_items`, `dict_values`) that are dynamic projections.
Which dict views behave like sets (Python)?	`dict_keys` and `dict_items` support set operations like `&`, `|`, `-`, `^`.
When do set operations on `dict_items` fail (Python)?	If any value is unhashable (because items are `(key, value)` pairs that must be hashable).
How can dict key insertion order matter (Python ≥ 3.7)?	Iteration preserves insertion order as a language feature (implementation detail since CPython 3.6).
How does `OrderedDict` equality differ from `dict` (Python)?	`OrderedDict` equality checks both keys/values and order.
What does `OrderedDict.move_to_end(k)` do (Python)?	Repositions an existing key to one end (useful for LRU-like logic).
What is `collections.ChainMap` (Python)?	A wrapper that searches multiple mappings in order as if they were one.
Where do updates go in a `ChainMap` (Python)?	Assignments/updates affect only the first (leftmost) mapping.
What does `Counter.update(iterable)` do (Python)?	It adds counts for elements from the iterable (instead of overwriting like `dict.update`).
Why are set literals like `{1, 2, 3}` preferred over `set([1, 2, 3])` (Python)?	They’re more readable and avoid building an intermediate list (often faster).
Do set infix operators like `a & b` accept any iterables (Python)?	No; infix operators require sets on both sides (methods like `.intersection(...)` accept iterables).
How can you remove duplicates while preserving first-seen order (Python ≥ 3.7)?	Use a dict: `list(dict.fromkeys(items))` (dict preserves insertion order).
Why is set membership testing fast (Python)?	Hash table lookup finds candidates by hash code with few probes on average.
Python 3: what does `str` represent?	Unicode text (a sequence of Unicode characters/code points).
Python 3: what does `bytes` represent?	An immutable sequence of bytes (integers 0–255).
Python 3: what does `bytearray` represent?	A mutable sequence of bytes (integers 0–255).
In Python 3, what does indexing a `bytes` or `bytearray` return?	An `int` (0–255), not a 1-character string.
In Python 3, what does slicing a `bytes` or `bytearray` return?	A new binary sequence of the same type (even for length-1 slices).
How do you create bytes from text in Python?	Encode a `str`, e.g. ``'café'.encode('utf-8')`` or ``bytes('café', encoding='utf-8')``.
How do you convert bytes to text in Python?	Decode, e.g. ``b'caf\\xc3\\xa9'.decode('utf-8')``.
What is a Unicode code point?	A number identifying a Unicode character (written like `U+0041`, up to `U+10FFFF`).
What is an encoding (Unicode context)?	An algorithm mapping code points ↔ byte sequences (e.g., UTF-8, UTF-16LE).
Why is `len(str)` vs `len(bytes)` often different?	`len(str)` counts Unicode characters; `len(bytes)` counts raw bytes in a particular encoding.
What string-like methods work on `bytes`/`bytearray` (high level)?	Most `str` methods that don’t require Unicode data or formatting; they operate on bytes values.
What’s a common gotcha for `bytes` methods vs `str` methods?	They generally expect `bytes` arguments (not `str`) for things like `.replace(...)`.
Name 3 common ways to build `bytes`/`bytearray` (Python).	From a `str` + encoding; from an iterable of ints 0–255; from a buffer-protocol object (copying its bytes).
What happens when you create `bytes(...)` from a buffer-like object (Python)?	It copies the underlying bytes (it does not share memory).
Which exception do you usually get when encoding `str` → `bytes` fails?	`UnicodeEncodeError`.
Which exception do you usually get when decoding `bytes` → `str` fails?	`UnicodeDecodeError`.
Why is `errors='ignore'` usually a bad idea when encoding?	It silently drops unencodable characters (data loss).
What is “mojibake”?	Garbled text caused by decoding bytes with the wrong encoding.
Why can decoding with the wrong legacy 8-bit encoding be dangerous?	It can succeed without errors but produce mojibake (silent corruption).
Can you reliably discover the encoding of arbitrary bytes?	Not in general; you usually must be told (metadata/protocol/file format).
Heuristic: why is UTF-8 “hard to mis-decode” compared to many encodings?	Random/wrong bytes are unlikely to form valid UTF-8, so decoding often raises `UnicodeDecodeError` instead of mojibake.
What is the “Unicode sandwich” best practice?	Decode bytes → `str` as early as possible; process text as `str`; encode to bytes as late as possible.
Why should you pass `encoding=` when opening text files in Python?	Default encodings vary by platform/locale and can change, causing mojibake or errors.
Why can a file contain more bytes than characters written (UTF-8 example)?	Some characters encode to multiple bytes (e.g., `'é'` is 2 bytes in UTF-8).
What determines the default encoding for `open()` in text mode (high level)?	Locale/user preferences (e.g., `locale.getpreferredencoding()`).
Why do encoding defaults bite Windows users more often (high level)?	Different defaults may be used for files vs console vs redirected I/O, and many defaults are non-UTF code pages.
What is a BOM (byte order mark)?	A leading byte sequence that signals endianness/encoding for multibyte encodings (notably UTF-16/UTF-32).
Unicode normalization: why can visually identical strings compare unequal?	They can use different code point sequences (e.g., composed vs combining forms).
What does `unicodedata.normalize('NFC', s)` do?	Composes characters to a canonical, typically shorter form (good for storage/comparison).
What does `unicodedata.normalize('NFD', s)` do?	Decomposes characters into base chars + combining marks (useful for stripping marks).
What’s the key risk of `NFKC` / `NFKD` normalization?	It applies compatibility decompositions that can lose/distort meaning (use for search/indexing, not storage).
What is case folding in Python (`str.casefold()`)?	A stronger, Unicode-aware lowercasing for case-insensitive comparisons.
Why can `casefold()` differ from `lower()` (example)?	It can map `'ß'` → `'ss'` and normalize some compatibility characters.
How can you strip diacritics from text (high level)?	Normalize to NFD, remove combining marks, then normalize back to NFC.
Why might you only strip diacritics for Latin letters?	To avoid mangling non-Latin scripts where removing marks changes the script more drastically.
Why does naive Unicode sorting often look “wrong”?	Python compares strings by code point order, not locale/collation rules.
How do you do locale-aware sorting with the standard library (Python)?	Set a locale and use `locale.strxfrm` as `key=` when sorting.
Why is `locale.setlocale` tricky in libraries?	Locale is process-global and depends on OS-installed locale data.
What does the Unicode database provide (high level)?	Character metadata (names, categories, numeric values, case folding data, etc.).
Regex: how do `\\d` and `\\w` differ for `str` vs `bytes` patterns (Python)?	For `str`, they match Unicode digits/word chars; for `bytes`, they’re ASCII-only.
In `re`, how can you force ASCII-only matching on `str` patterns?	Use the `re.ASCII` flag.
What happens if you call `os.listdir(b'.')` (Python)?	It returns filenames as `bytes` (raw OS bytes), not `str`.
What do `os.fsencode` / `os.fsdecode` do?	Convert between `str` and `bytes` filenames using the filesystem encoding (handling `PathLike` too).
What is a “data class” (design pattern, Python)?	A record-like class: mostly fields, little or no behavior.
Name 3 stdlib “data class builders” (Python).	`collections.namedtuple`; `typing.NamedTuple`; `@dataclasses.dataclass`.
What boilerplate do data class builders typically generate (Python)?	Methods like `__init__`, `__repr__`, `__eq__` (and sometimes ordering, etc.).
Are `namedtuple` / `typing.NamedTuple` instances mutable (Python)?	No; they’re tuple-based (immutable).
What does `@dataclass(frozen=True)` do (Python)?	Disallows setting/deleting fields after init (raises `FrozenInstanceError`).
Are `namedtuple` / `typing.NamedTuple` classes tuple subclasses (Python)?	Yes.
Which data class builders support `class` statement syntax (Python)?	`typing.NamedTuple` and `@dataclass`.
How do you convert a namedtuple/NamedTuple instance to a dict (Python)?	`x._asdict()`.
How do you convert a dataclass instance to a dict (Python)?	`dataclasses.asdict(x)`.
How do you get field names from a namedtuple/NamedTuple class (Python)?	`Cls._fields`.
How do you get default values from a namedtuple/NamedTuple class (Python)?	`Cls._field_defaults` (mapping of field → default).
How do you get dataclass field metadata (Python)?	`dataclasses.fields(x_or_cls)` (returns `Field` objects).
How do you make a modified copy of a namedtuple instance (Python)?	`x._replace(**kwargs)`.
How do you make a modified copy of a dataclass instance (Python)?	`dataclasses.replace(x, **kwargs)`.
How do you create a namedtuple class at runtime (Python)?	`collections.namedtuple('Name', 'a b ...')`.
`namedtuple`: how can you pass the field names (Python)?	As an iterable of strings or a single space-delimited string.
`namedtuple`: how are field values passed to the constructor (Python)?	As separate positional args (or keywords), not as one iterable.
`namedtuple`: how do you build an instance from an iterable (Python)?	`Cls._make(iterable)` (like `Cls(*iterable)`).
Since Python 3.7, how do you set defaults in `namedtuple` (Python)?	Use `namedtuple(..., defaults=[...])` (defaults apply to rightmost fields).
What’s the main difference of `typing.NamedTuple` vs `collections.namedtuple` (Python)?	It includes field type annotations (for static checking/IDE help).
In a plain class, what does `a: int` do (Python)?	Adds to `__annotations__` only; it does not create attribute `a`.
In a plain class, what does `b: float = 1.1` do (Python)?	Records an annotation and creates a class attribute `b = 1.1`.
In `typing.NamedTuple`, what does `a: int` become (Python)?	An instance field (read-only attribute; tuple-based).
In a dataclass, what does `a: int` become (Python)?	An instance attribute assigned by the generated `__init__`.
Where do `NamedTuple`/dataclass store field annotations (Python)?	In the class `__annotations__` mapping.
List the most-used `@dataclass` options (Python).	`init`, `repr`, `eq`, `order`, `unsafe_hash`, `frozen`.
What does `order=True` generate in a dataclass (Python)?	Ordering methods: `__lt__`, `__le__`, `__gt__`, `__ge__`.
Dataclass: when is `__hash__` generated by default (Python)?	When `eq=True` and `frozen=True` (otherwise it’s typically unhashable).
What is `dataclasses.field(...)` used for (Python)?	Per-field configuration (defaults, repr/compare behavior, etc.).
How do you set a per-instance mutable default in a dataclass (Python)?	Use `field(default_factory=...)` (e.g., `list`).
Why does `@dataclass` reject `guests: list = []` (Python)?	Mutable defaults would be shared across instances; use `default_factory`.
What does `default_factory` do (dataclass, Python)?	Calls a 0-arg callable to create a fresh default value per instance.
Dataclass rule about defaults ordering (Python)?	After a field with a default, all following fields must also have defaults.
What is `__post_init__` for (dataclass, Python)?	Validation and/or computing derived values after initialization.
Why can annotating a dataclass class attribute be tricky (Python)?	An annotation can make it look like an instance field unless marked as `ClassVar`.
How do you declare a typed class variable in a dataclass (Python)?	`from typing import ClassVar`; then `x: ClassVar[T] = ...`.
What does `ClassVar[...]` signal to `@dataclass` (Python)?	It is not an instance field.
What is an init-only variable in dataclasses (Python)?	A constructor argument that isn’t stored as a field.
How do you declare an init-only variable (dataclass, Python)?	`from dataclasses import InitVar`; then `db: InitVar[DbType]`.
What do class patterns in `match`/`case` match (Python)?	Instances by type and optionally by attribute values.
What enables positional class patterns for a class (Python)?	`__match_args__` listing attribute names in positional-match order.
Python variables: are they “boxes” or “labels”?	Labels (names bound to objects), not boxes that contain copies.
If b = a and a refers to a mutable object, what happens to b when you mutate through a?	b reflects the same change because both names reference the same object.
Why is `is` typically faster than `==` (Python)?	`is` can’t be overloaded; it’s essentially an identity comparison.
When is `is` the right tool in Python code?	Comparing to singletons like `None` or a sentinel object.
Idiom: how do you test for `None` in Python?	`x is None` (and `x is not None`).
How do you create a sentinel object for identity checks (Python)?	`SENTINEL = object()` and compare with `is`.
Common pitfall: when is using `is` usually wrong?	For value equality like strings/integers/collections; use `==` instead.
What does tuple immutability actually guarantee (Python)?	The tuple’s stored references don’t change; referenced mutable objects can still mutate.
How can a tuple “value” appear to change even if the tuple is immutable?	If it references a mutable item that changes in place.
How do you make a shallow copy of a list (common ways)?	`list(x)`, `x[:]`, or `x.copy()`.
In a shallow copy, what happens if you mutate a shared inner list?	The mutation is visible through both the original and the shallow copy.
How do you make shallow vs deep copies of arbitrary objects (Python)?	`copy.copy(x)` vs `copy.deepcopy(x)`.
Why can `deepcopy` handle cyclic references safely (Python)?	It memoizes objects already copied to avoid infinite recursion.
Why can a deep copy be undesirable (Python)?	It may be too expensive or may copy things that shouldn’t be copied (resources/singletons).
How can a class customize copying behavior (Python)?	Implement `__copy__` and `__deepcopy__`.
What is Python’s parameter passing model called?	Call by sharing.
Under call by sharing, what does a function receive for each argument?	A copy of the reference to the same object.
What can a function do to a mutable argument under call by sharing?	Mutate the object, affecting the caller’s view.
What can a function not do to the caller’s bindings under call by sharing?	It can’t rebind the caller’s name to a different object.
Mutating vs rebinding: what’s the difference inside a function (Python)?	Mutation affects the shared object; rebinding only changes the local parameter name.
Augmented assignment (`+=`): what’s the high-level rule for mutables vs immutables?	Mutables often change in place; immutables create a new object and rebind.
Why can `a += b` differ for lists vs tuples (Python)?	Lists can mutate in place; tuples are immutable so a new tuple is created.
When are default parameter values evaluated (Python)?	Once at function definition time (typically module import).
Where are positional default parameter values stored (Python)?	On the function object (e.g., `func.__defaults__`).
Why are mutable default arguments dangerous (Python)?	Mutations persist and affect future calls that use the default.
Safe pattern for optional mutable parameters (Python)?	Default to `None`, then create a new list/dict inside the function.
Why use `if x is None:` for optional parameters (Python)?	Distinguishes “argument omitted” from an empty container provided by the caller.
How do you defensively create your own list from an iterable argument?	`list(iterable)`.
When can an object be garbage-collected (Python, high level)?	When it becomes unreachable (no strong references).
In CPython, what primary mechanism destroys most objects promptly?	Reference counting (plus a cycle detector for cyclic garbage).
What problem does cyclic garbage collection address (CPython)?	Unreachable reference cycles that reference counting alone can’t free.
Is destruction timing guaranteed across Python implementations?	No; you should not rely on immediate finalization.
What is `__del__` for (Python)?	A finalizer hook when an object is about to be destroyed; tricky and rarely needed.
Why is relying on `__del__`/GC timing risky?	Finalization may be delayed or behave differently across implementations/cycles.
What is a weak reference (Python)?	A reference that doesn’t keep the target alive (doesn’t increase refcount).
What does `weakref.finalize(obj, callback)` do (Python)?	Registers `callback` to run when `obj` is garbage-collected, without keeping `obj` alive.
What does “functions are first-class objects” mean (Python)?	Functions can be created at runtime; assigned; stored; passed as args; returned from functions.
`sorted(..., key=...)`: what is `key` for (Python)?	A 1-arg function used to compute sort keys for each item.
Modern Python: what often replaces `map` and `filter` in simple pipelines?	Comprehensions; use a list comprehension or generator expression, often with an `if` clause.
What is the most common “reduce-like” task, and its preferred built-in (Python)?	Summation; use `sum(...)`.
What restriction applies to the body of a `lambda` (Python)?	It must be a single expression (no statements like `while`, `try`, `=` assignment).
Why is `lambda` usually best kept small in Python?	Nontrivial lambdas get hard to read; prefer a named `def`.
What makes an instance callable (Python)?	Defining a `__call__(self, ...)` method on its class.
Why are callable instances useful (Python)?	They can keep internal state across calls (function-like objects with memory).
What is a common `__call__` pattern for a stateful object (Python)?	`__call__` delegates to a regular method (e.g., `return self.pick()`).
When you call a class, which methods are involved (Python, high level)?	`__new__` creates the instance; `__init__` initializes it.
What do `*args` capture in a function definition (Python)?	Extra positional arguments, collected into a tuple.
What do `**kwargs` capture in a function definition (Python)?	Extra keyword arguments, collected into a dict.
How do you unpack an iterable into positional arguments in a call (Python)?	Use `*iterable`, e.g. `fn(*args_list)`.
How do you unpack a mapping into keyword arguments in a call (Python)?	Use `**mapping`, e.g. `fn(**kwargs_dict)`.
What is a keyword-only argument (Python)?	A parameter that can only be passed by keyword, not position.
How do you declare keyword-only parameters (Python)?	Place them after `*args` or after a bare `*` in the signature.
How do you declare keyword-only params without `*args` (Python)?	Use a bare `*`, e.g. `def f(a, *, b): ...`.
Can a keyword-only parameter be required (Python)?	Yes; omit its default value.
What are positional-only parameters (Python ≥ 3.8)?	Parameters that must be passed positionally, not by keyword.
How do you declare positional-only parameters in Python (syntax)?	Use `/` in the parameter list; parameters left of `/` are positional-only.
Why do positional-only parameters exist (common rationale)?	To match/lock an API and avoid exposing parameter names as part of the call contract.
Which built-in is a classic example of positional-only args (Python)?	`divmod(a, b)` (can’t be called as `divmod(a=..., b=...)`).
What module provides function equivalents for many operators (Python)?	`operator`.
Why use `operator.mul` with `reduce` (Python)?	Avoid trivial lambdas like `lambda a,b: a*b`; improves readability.
What does `operator.itemgetter(n)` produce (Python)?	A function that fetches item `obj[n]` from its argument.
Why can `itemgetter` work with mappings too (Python)?	It uses `__getitem__`, which dicts and many other types support.
What does `itemgetter(i, j, ...)` return (Python)?	A function returning a tuple of extracted items.
What does `functools.partial(func, *args, **kwargs)` return (Python)?	A new callable with some arguments pre-bound.
Why is `partial` useful (Python)?	Adapts a callable to an API expecting fewer/different arguments (callbacks).
How can `partial` help with repeated normalization patterns (Python example)?	Bind one arg of a function to create a specialized helper, e.g. `nfc = partial(unicodedata.normalize, 'NFC')`.
What is “gradual typing” (Python typing)?	Type hints are optional; unannotated code can be treated as `Any` by the checker; checking is static-only (no runtime enforcement).
In gradual typing, what happens when the checker can’t determine a type (Python typing)?	It assumes `Any`.
Do type hints prevent wrong types from being passed at runtime (Python)?	No; they’re ignored by the runtime and only used by tools (type checkers/IDEs/linters).
What does “types are defined by supported operations” mean (typing perspective)?	A type is characterized by what operations/methods are valid on values of that type.
What is duck typing (Python concept)?	Correctness is determined at runtime by whether an object supports the operations you use (not by declared types).
What is nominal typing (static type checking perspective)?	Variables/parameters have declared types; only operations defined on the declared type are considered valid by the checker.
Why can static checking reject code that “works at runtime” (Python typing)?	The checker only trusts declared interfaces; runtime may pass a subtype that happens to support extra operations.
How is `Any` different from `object` (Python typing)?	`object` accepts any value but supports only `object`’s operations; `Any` is treated as supporting any operation.
Why can annotating something as `Any` reduce the value of type checking?	It suppresses many errors because the checker assumes any operation is valid.
What does “subtype-of” mean in nominal typing?	If `T2` is a subtype of `T1`, you can use a `T2` wherever `T1` is expected (Liskov substitution).
What does “consistent-with” add beyond subtype-of (gradual typing)?	It includes subtype relationships and also treats `Any` as matching every type in both directions.
Python 3.10+: what does `T | U` mean in annotations?	A union type (equivalent to `Union[T, U]`).
Why can returning `Union[...]` be annoying for callers (Python typing)?	Callers must narrow/check the runtime type before using the result safely.
Python 3.9+: what does `list[str]` mean in a return type annotation?	A list where every element is a `str` (generic type parameter is `str`).
How do you annotate a “record-like” tuple with fixed field types (Python typing)?	Use `tuple[T1, T2, ...]` (e.g., `tuple[float, float]`).
How do you annotate a variable-length tuple of one element type (Python typing)?	Use `tuple[T, ...]` (ellipsis) for “any length” tuples of `T`.
How do you annotate a mapping type (Python typing)?	`Mapping[KeyType, ValueType]` (or concrete `dict[KeyType, ValueType]` when you require `dict`).
Why prefer `collections.abc.Mapping` over `dict` for a parameter (Python typing)?	It accepts more mapping implementations (e.g., `dict`, `defaultdict`, `ChainMap`, `UserDict`), improving caller flexibility.
Why prefer concrete collection types for return annotations (Python typing)?	The function returns a concrete object; concrete return types are more informative and usable than vague abstractions.
Why might you need to annotate a local variable even when the expression is obvious to you (Python typing)?	Type checkers may not infer the desired generic type; an explicit annotation can resolve “need type annotation” errors.
What is a “type alias” (Python typing)?	A named alias for a type expression to improve readability (e.g., `FromTo = tuple[str, str]`).
When should you annotate an input as `Iterable[T]` instead of `Sequence[T]` (Python typing)?	When you only need to iterate and want to accept generators/streams, not just sized/indexable sequences.
When should you annotate an input as `Sequence[T]` instead of `Iterable[T]` (Python typing)?	When you need `len(...)` and/or indexing/slicing behavior.
Why is `Iterable[T]` often too vague as a return type (Python typing)?	It doesn’t promise key properties (re-iterable vs one-shot, concrete container type), making downstream use less clear.
What is a `TypeVar` used for (Python typing)?	To define a type parameter so input types can be reflected in output types (parameterized generics).
What is a restricted `TypeVar` (Python typing)?	A type variable constrained to one of a fixed set of types listed in `TypeVar(...)`.
What is a bounded `TypeVar` (Python typing)?	A type variable constrained to be a subtype/consistent-with an upper bound declared via `bound=...`.
Why is `TypeVar(..., bound=Hashable)` useful (Python typing)?	It ensures elements are hashable (usable in dict/Counter) while preserving the specific element type in the return type.
What is `typing.Protocol` for (Python typing)?	Defining structural interfaces so a type checker can verify “supports these methods” without requiring inheritance.
What is “static duck typing” (Python typing)?	Using `Protocol` to express operation-based requirements (structural typing) for static type checkers.
When is a concrete type consistent-with a protocol type (Python typing)?	When it implements the protocol’s required methods with compatible signatures.
Why are protocols useful for annotating “supports operation X” (Python typing)?	ABCs may not include the needed method; a protocol can describe the exact operation (e.g., `__lt__`) the algorithm relies on.
What does `Callable[[A, B], R]` mean (Python typing)?	A callable that takes arguments of types `A` and `B` and returns type `R`.
How do you annotate “any callable signature” (Python typing)?	Use `Callable[..., R]`.
What is `NoReturn` used for (Python typing)?	Return annotation for functions that never return normally (e.g., always raise or call `sys.exit()`).
Decorator desugaring: what does `@deco` mean (Python)?	It’s syntax sugar for rebinding: `func = deco(func)` right after `func` is defined.
Do decorators run at call time or at definition/import time (Python)?	At function definition time; usually during module import (module load), not when the function is later called.
Import time vs runtime: what’s the difference (Python, decorators context)?	Import time is when module top-level code runs (decorators execute); runtime is when decorated functions are invoked later.
What is a registration decorator used for (Python)?	To add functions to a registry (e.g., routing tables, plugin hooks) at import time.
What is a stacked decorator (Python)?	Multiple decorators applied to one function using multiple `@...` lines.
Why do naive decorators often break `__name__` and `__doc__` (Python)?	Because the wrapper replaces the function object, so its metadata becomes the wrapper’s metadata.
Why should a decorator wrapper accept `*args, **kwargs` (Python)?	To forward any positional/keyword arguments so the decorator works for many signatures.
Variable scope rule: when is a name local inside a function (Python)?	If it’s a parameter or it’s assigned anywhere in the function body (unless declared `global`/`nonlocal`).
Why does `print(b); b = 9` raise `UnboundLocalError` (Python)?	Because assignment makes `b` local for the whole function, so `print(b)` reads an uninitialized local.
In a closure, where are captured bindings stored (Python)?	In the returned function object’s closure cells (`fn.__closure__`), keyed by names in `fn.__code__.co_freevars`.
Why does rebinding an outer-scope name require `nonlocal` (Python)?	Without `nonlocal`, an assignment makes the name local, so the closure binding won’t be updated.
Why does `count += 1` inside a nested function create a local `count` without `nonlocal` (Python)?	Augmented assignment on immutables rebinding (`count = count + 1`) counts as assignment, so the compiler treats `count` as local.
What’s the difference between mutating a captured list and rebinding a name (Python closures)?	`series.append(x)` mutates the same object (no rebinding); `count = count + 1` rebinds the name (needs `nonlocal` to target outer scope).
Variable lookup order: where does Python look if a name isn’t local (Python)?	Enclosing function scopes (nonlocal) → module globals → `__builtins__` (if not found, `NameError`).
What is the `dis` module used for (Python)?	Disassembling Python bytecode to inspect how the compiler treats names and operations.
What does `functools.cache` implement (Python)?	Memoization: caching results of function calls to avoid repeated computation for the same arguments.
`functools.cache`: what’s the key requirement on arguments (Python)?	All arguments must be hashable, because the cache is keyed using a dict.
What is a risk of using `functools.cache` in long-running processes (Python)?	Unbounded growth: it can consume a lot of memory if there are many distinct argument combinations.
What does `functools.lru_cache` add compared with `cache` (Python)?	A bounded cache with eviction controlled by `maxsize` (least-recently-used policy).
`lru_cache`: what does `maxsize` control (Python)?	The maximum number of cached entries (older entries are evicted once full).
What does `functools.singledispatch` create (Python)?	A single-dispatch generic function: multiple implementations chosen by the type of the first argument.
In single dispatch, which argument is used to choose the implementation (Python)?	The first positional argument’s runtime type.
What does `@singledispatch` decorate (Python)?	The base (fallback) implementation, which typically handles `object` as a catch-all.
How do you register a specialized implementation (singledispatch, Python)?	Decorate it with `@<base>.register` (using a first-parameter annotation or passing an explicit type to `register`).
In `singledispatch`, which implementation is chosen when multiple match (Python)?	The most specific matching type (best match in the type hierarchy), not the first one defined.
Can `singledispatch` registrations be defined in other modules (Python)?	Yes; registrations can live anywhere, allowing modular extension.
Why is `singledispatch` more extensible than an `if/elif` chain (Python)?	Because new types can register new implementations without editing the central dispatcher function.
How do you register a type without annotating the first parameter (singledispatch, Python)?	Pass the type to the decorator: `@<base>.register(SomeType)`.
How can one function implementation handle multiple types (singledispatch, Python)?	Stack multiple `@<base>.register(Type)` decorators on the same function.
Why register ABCs instead of concrete types in `singledispatch` (Python)?	It supports a broader range of compatible types (including virtual subclasses) without listing each concrete class.
What is a parameterized decorator (Python)?	A decorator that accepts extra arguments; implemented as a decorator factory that returns the actual decorator.
How does Python apply `@deco(arg)` (Python)?	It calls the factory `deco(arg)` to get a decorator, then applies it: `func = deco(arg)(func)`.
How can a decorator factory enable toggling behavior like registration (Python)?	The factory captures parameters (e.g., `active`) in a closure and returns a decorator that acts based on those parameters.
Class-based decorator factory: what makes an instance act as a decorator (Python)?	Implementing `__call__(self, func)` so the instance can be used as the decorator.
Classic Strategy in Python: why use an ABC + `@abstractmethod`?	To make the pattern explicit and enforce that concrete strategies implement the required method.
Function-oriented Strategy: when can you replace strategy classes with functions (Python)?	When each strategy has no internal state and effectively implements a single method (one algorithm).
Why doesn’t method auto-binding apply to a callable stored in an instance attribute (Python)?	Method binding is a descriptor feature of functions defined on the class; a plain callable attribute isn’t automatically bound.
How can you collect promo functions using `globals()` (Strategy pattern)?	Filter `globals().items()` by naming convention (e.g., names ending with `_promo`) and collect the callable values.
What is a downside of collecting strategies by name convention (e.g., suffix `_promo`)?	It’s implicit and brittle: naming mistakes or unrelated functions can be accidentally included/excluded.
Why can putting all strategy functions in a separate module help (Strategy pattern)?	It centralizes strategies and enables introspection without relying on naming conventions in unrelated code.
What does `inspect.getmembers(obj, predicate)` do (Python)?	Returns an object’s attributes as `(name, value)` pairs, optionally filtered by a predicate.
How can you build a `promos` list by introspecting a `promotions` module (Python)?	Use `inspect.getmembers(promotions, inspect.isfunction)` and collect the returned functions.
What is a registration decorator (Python)?	A decorator that records/registers functions (e.g., appends them to a list) as a side effect at import time.
In decorator-enhanced Strategy, what does `@promotion` do?	It registers the decorated promotion function (adds it to a `promos` list) and returns it unchanged.
Why does a registration decorator often return the original function (Python)?	So the decorated function’s callable behavior stays the same while still being registered.
Registration decorator benefit: why avoid name conventions for strategies?	Registration is explicit: strategies don’t need special suffixes and accidental inclusion is less likely.
Registration decorator benefit: how can you disable a strategy quickly (Python)?	Comment out or remove the `@promotion` decorator on that function.
Registration decorator benefit: can strategies be registered from other modules (Python)?	Yes; as long as they import and use the `@promotion` decorator, they’ll be registered when that module loads.
Command pattern: why is Command described as an OO replacement for callbacks (GoF)?	It packages an action as an object so it can be stored, passed around, and invoked later.
In Python, what’s the simplest replacement for a single-method Command object?	Pass a function (or other callable) instead of a command instance.
How can MacroCommand be implemented idiomatically in Python (high level)?	As a callable object (`__call__`) that loops over and calls stored command callables.
Why might you still use a class for Command instead of a function (Python)?	To keep state, provide extra methods (e.g., undo), and encapsulate more complex behavior than a single callback.
What are two alternatives to classes for keeping state across calls (Python)?	A closure (captured free variables) or a callable instance (object with `__call__` + attributes).
When should you consider a closure for Command-like state (Python)?	When you need small, hidden state tied to a function and a minimal public surface.
When should you consider a callable instance for Command-like state (Python)?	When you want explicit, inspectable state and possibly extra methods beyond callability.
Why prefer a concrete return type for `best_promo` (Python typing)?	It always returns a concrete discount amount (e.g., `Decimal`), which is easier for callers than unions/optionals.
How do first-class functions reduce boilerplate in Strategy (Python)?	They replace single-method strategy classes/instances with plain functions passed around as values.
How do first-class functions reduce boilerplate in Command (Python)?	They allow actions to be represented as callbacks (callables) without creating a Command class hierarchy.
What uses `obj.__format__(format_spec)` in Python?	F-strings, `format()`, and `str.format()`.
How can a class support unpacking like `x, y = obj` (Python)?	By implementing `__iter__` so the object is iterable.
What is an alternative constructor in Python?	A class-level constructor, often implemented as a `@classmethod`, that builds instances in a different way than `__init__`.
Why is `@classmethod` useful for alternative constructors (Python)?	It receives the class as the first argument, so it can construct instances of the actual class being called.
How can a user-defined class support custom formatting codes in Python?	By implementing `__format__` and interpreting `format_spec` however the class chooses.
What makes an object hashable in Python?	It has a stable hash value during its lifetime and an equality relation consistent with that hash.
What is a common pattern for implementing `__hash__` on a value object (Python)?	Hash a tuple of the attributes that define equality, e.g. `hash((self.x, self.y))`.
How can read-only properties help a Python object act immutable?	They allow attribute reads but prevent normal reassignment after initialization.
What does `__match_args__` do in Python pattern matching?	It defines which attributes are used, and in what order, for positional class patterns.
What is name mangling in Python?	The rewriting of attributes like `__x` to `_ClassName__x` to reduce accidental name clashes.
What does `__slots__` do in a Python class?	It declares a fixed set of instance attributes and can replace per-instance `__dict__` storage with a more memory-efficient layout.
What is the main benefit of `__slots__` in Python?	Lower memory usage for large numbers of instances.
What limitation does `__slots__` impose on instances (Python)?	They can only have the attributes named in `__slots__`, unless `'__dict__'` is also included.
What must a subclass do if it also wants `__slots__` behavior (Python)?	Declare its own `__slots__`; otherwise subclass instances usually get a `__dict__`.
Why might you add `'__dict__'` to `__slots__` (Python)?	To allow dynamic attributes while still slotting the declared ones.
Why might you add `'__weakref__'` to `__slots__` (Python)?	To allow instances to be targets of weak references.
How do class attributes behave when accessed through an instance (Python)?	They act as default attribute values unless the instance has its own attribute of the same name.
What happens when you assign to an instance attribute that shadows a class attribute (Python)?	The instance gets its own attribute, leaving the class attribute unchanged.
How can you customize a default class attribute for all instances in Python?	Set it on the class or define a subclass that overrides it.
Why can a user-defined class behave like a sequence without inheriting from a sequence base class?	Python relies on protocols: if the required methods are present with the expected behavior, the object can be used as a sequence.
For a user-defined sequence type, why is composition often preferable to inheritance?	You can store data in an underlying built-in sequence and implement only the protocol methods you need.
What constructor style is considered best practice for a Python sequence type?	Take a single iterable argument rather than arbitrary positional items.
Why is `reprlib.repr` useful in a collection type’s `__repr__`?	It produces a safe abbreviated representation for large collections.
What is a key rule for implementing `__repr__`?	It should never raise an exception; it should always return some useful debugging representation.
Why can implementing only part of a protocol still be acceptable in Python?	If a specific use case needs only a subset of the protocol, supporting just that subset can be enough.
How does `obj[1:4]` reach a user-defined `__getitem__` method?	Python passes a `slice(1, 4, None)` object to `__getitem__`.
What attributes does a `slice` object expose?	`start`, `stop`, and `step`.
What does `slice.indices(len)` do?	It returns normalized nonnegative `(start, stop, step)` values for a sequence of the given length.
Why is `slice.indices()` useful when implementing slicing?	It handles missing, negative, and out-of-range bounds the same way built-in sequences do.
How should a Pythonic sequence type usually handle slicing?	A slice should return a new instance of the same sequence type, not an unrelated container type.
Why is `operator.index(x)` better than `int(x)` for validating sequence indexes?	It accepts only objects that are valid integer indexes, whereas `int(x)` may coerce values like floats.
When is `__getattr__` called in Python?	Only as a fallback when normal attribute lookup fails.
Why can implementing `__getattr__` without `__setattr__` cause inconsistent behavior?	Assigning an instance attribute can bypass the fallback lookup and shadow the virtual attribute logic.
When is it common to implement `__setattr__` together with `__getattr__`?	When virtual or computed attributes should remain read-only or behave consistently.
How can `functools.reduce` be used in a sequence `__hash__` implementation?	Reduce the hashes of all components into one aggregate hash, such as with `operator.xor`.
What is a key pitfall of `zip` in Python?	It stops at the shortest iterable without warning.
Why should an equality check using `zip` usually compare lengths first?	Because `zip` alone can ignore extra items in the longer operand.
How can `all(a == b for a, b in zip(self, other))` improve a sequence `__eq__` implementation?	It compares items lazily and stops at the first mismatch.
What is goose typing (Python)?	Runtime type checking against abstract base classes (ABCs) instead of concrete classes.
What is a static protocol (Python)?	An explicit structural interface defined with a `typing.Protocol` subclass for static type checkers.
Dynamic protocol vs static protocol: what is one key difference?	A dynamic protocol can still be useful when only partly implemented; a static protocol must provide every required member to satisfy the checker.
Why can the `in` operator work on some objects without `__contains__`?	Python can fall back to iteration, and sequence-like objects may be iterated via `__getitem__`.
What is monkey patching (Python)?	Changing a module, class, or function at runtime to add or alter behavior without editing its original source.
Why can monkey patching make an object support a protocol at runtime?	Protocols depend on available methods, so adding the needed method can make existing code accept the object.
Why is `list(iterable)` a useful fail-fast pattern in Python?	It immediately verifies the argument is iterable and gives you a concrete list to store or process.
When is `iter(x)` a good fail-fast check in Python?	When any iterable is acceptable and you want an immediate error if `x` is not iterable.
When is an `isinstance` check against an ABC reasonable in Python?	When you need to enforce an interface contract at runtime without tying the code to one concrete class.
What is a virtual subclass of an ABC (Python)?	A class recognized by an ABC through registration even though it does not inherit from that ABC.
What does `ABC.register(SomeClass)` do (Python)?	It makes `SomeClass` a virtual subclass for `isinstance` and `issubclass` checks.
What does a virtual subclass of an ABC not get from registration?	It does not inherit methods, attributes, or appear in the normal method resolution order of the ABC.
Why are the ABCs in `collections.abc` useful?	They provide explicit runtime interfaces, reusable mixin methods, and better abstraction for type hints than concrete collection classes.
What is `__subclasshook__` used for in an ABC?	To let an ABC recognize structurally compatible classes as subclasses without explicit inheritance or registration.
How can an ABC support structural typing at runtime?	By implementing `__subclasshook__` to approve classes that provide the required methods.
Why should `__subclasshook__` usually be used sparingly in custom ABCs?	Method-name checks alone are often too weak to guarantee the intended semantics of a richer interface.
What does `@runtime_checkable` do for a `typing.Protocol`?	It allows the protocol to be used with `isinstance` and `issubclass` at runtime.
What is a limitation of runtime checks against static protocols?	They usually check only whether the required attributes exist, not whether signatures or type annotations are actually compatible.
Why are narrow static protocols often better than broad ones?	They are easier to satisfy, easier to reuse, and impose fewer unnecessary requirements on clients.
Numeric ABCs vs numeric static protocols: what is the main tradeoff in Python?	`numbers` ABCs are useful for runtime checks, while `typing` numeric protocols work better with static type checkers.
What is the recommended way to call an overridden superclass method in Python?	Use `super()` instead of naming the base class directly.
Why is `super()` preferred over calling a superclass method by class name in Python?	It avoids hardcoding a specific base class and supports multiple inheritance correctly.
Why is calling `super().__init__(...)` especially important in Python subclasses?	Python does not automatically call a superclass initializer, so subclasses must do it explicitly when needed.
What does `super()` return in Python?	A dynamic proxy that finds the next matching method in the method resolution order and binds it to the current receiver.
What do the optional arguments to `super(type, object_or_type)` represent in Python?	The class where lookup should start and the instance or class the resolved method should bind to.
What problem does `super()` help solve in multiple inheritance?	It lets methods cooperate along the method resolution order instead of hardwiring one parent call path.
Why is subclassing built-in types like `dict` or `list` directly tricky in CPython?	Built-in methods often bypass user-defined overrides when delegating internally.
Why can overriding `__setitem__` in a `dict` subclass fail to affect `dict.__init__` or `dict.update`?	Those built-in methods may operate through C-level implementations that do not call the override.
Why can overriding `__getitem__` in a `dict` subclass fail to affect `dict.update`?	The built-in update logic may bypass late-bound method lookup and read the underlying data directly.
Why are `UserDict`, `UserList`, and `UserString` often better subclassing targets than built-ins?	They are designed for extension and cooperate with Python-level method overrides.
What object-oriented principle is violated when built-in methods bypass subclass overrides?	Late binding, where method lookup should start from the receiver's actual class.
What is multiple inheritance in Python?	The ability of a class to inherit from more than one base class.
What problem must a language solve when supporting multiple inheritance?	It must resolve conflicts when several base classes provide attributes or methods with the same name.
What is the method resolution order (MRO) in Python?	The ordered sequence of classes Python searches when looking up methods and attributes.
Where can you inspect a class's MRO in Python?	In the class `__mro__` attribute.
What is a cooperative method in Python inheritance?	A method that calls `super()` so other implementations in the MRO can participate.
Why can a noncooperative method break multiple inheritance?	It can stop the call chain early and prevent later classes in the MRO from running.
What signature requirement helps cooperative multiple inheritance work?	Cooperative methods should have compatible signatures because different classes may be called in different orders.
Why can changing the order of base classes change behavior in Python multiple inheritance?	The base-class order affects the MRO and therefore which method `super()` reaches next.
What is a mixin class in Python?	A class meant to be combined with other base classes to add a focused behavior, not used as the sole concrete base class.
What is the main purpose of a mixin class?	Code reuse by adding a small, specific behavior to unrelated classes.
Why should a mixin usually appear before other concrete bases in a Python class definition?	So its methods appear early enough in the MRO to intercept and delegate behavior as intended.
Why should mixins usually avoid keeping internal instance state?	State makes them harder to combine safely with unrelated classes in multiple inheritance.
Why is the `Mixin` suffix recommended in Python class names?	It makes the class's intended role explicit to readers and users.
Why can aggregate classes be useful to library users?	They hide tricky base-class ordering and expose a sensible precomposed class.
Why should interfaces be explicit with ABCs or protocols in modern Python?	It makes subtype intent clearer than relying on concrete inheritance alone.
Why is inheritance for code reuse often weaker than composition?	Inheritance tightly couples classes, while composition can reuse behavior with less brittleness.
Why is subclassing concrete classes riskier than subclassing ABCs or mixins?	Concrete classes often have internal state and assumptions that method overrides can break.
What is a common design warning sign in inheritance-heavy code?	Tall or complex class hierarchies that are hard to understand and fragile to change.
What is the practical guidance for inheritance in Python applications?	Avoid it when composition or functions are sufficient, and subclass mainly where frameworks or explicit extension points expect it.
What problem do overloaded signatures solve (Python typing)?	They let a type checker model different valid call shapes precisely, especially when the return type depends on the arguments.
How are overloaded signatures written in a regular Python module?	Write one or more `@overload` stubs before the real implementation, then provide one actual function body.
How does a type checker choose among overloaded signatures (Python typing)?	It tries to match the call against the overloads in order and uses the first matching signature.
Why can flexible Pythonic APIs need many overloads (Python typing)?	Because one function may accept several argument patterns whose parameter and return types relate differently.
What is `TypedDict` used for (Python typing)?	To describe a `dict` used as a record with a fixed set of string keys and specific value types per key.
What does `TypedDict` provide to a type checker?	Field-specific key and value types for dict-like records, so invalid keys or value types can be flagged statically.
What runtime effect does `TypedDict` have?	Essentially none; it exists for static type checking, and its constructor produces a plain `dict`.
What kinds of mistakes can a type checker catch with `TypedDict`?	Wrong value types, invalid keys, and illegal deletion or assignment patterns for declared keys.
Why is `TypedDict` not a replacement for JSON validation?	Static typing cannot verify the runtime shape of dynamic data like `json.loads(...)`; only runtime validation can do that.
What does `typing.cast` do for the type checker?	It tells the checker to treat a value as a specified type even when inference cannot prove that type.
When is `cast` a reasonable tool (Python typing)?	When the checker lacks information or external type hints are wrong, but the programmer has stronger knowledge about the value.
Why is frequent use of `cast` a code smell?	It can hide real typing problems, poor APIs, or low-quality stubs instead of fixing the underlying issue.
Where does Python store annotations at runtime?	In `__annotations__` mappings on functions, classes, and modules.
Why is reading raw `__annotations__` directly fragile?	Runtime annotations may contain strings, unresolved forward references, or behavior that changes across Python versions.
What runtime problem do forward references create for annotations?	A type may need to be named before the class object exists, so runtime inspection may see a string instead of the real type.
What does `from __future__ import annotations` do?	It postpones annotation evaluation and stores annotations as strings instead of evaluating them at definition time.
What should you prefer over reading raw `__annotations__` directly?	`inspect.get_annotations(...)` or `typing.get_type_hints(...)`.
Why wrap `get_type_hints` or `get_annotations` in your own helper?	It localizes future compatibility changes so the rest of the codebase does not depend directly on unstable annotation-inspection details.
What is a generic type (Python typing)?	A type declared with one or more type variables, such as a class parameterized by element type.
What is a parameterized type (Python typing)?	A generic type supplied with actual type arguments, such as `LottoBlower[int]`.
When can a type parameter be covariant?	When it describes values that come out of the object, but are not later accepted back in.
When can a type parameter be contravariant?	When it describes values that go into the object, but are not produced back out.
When must a type parameter be invariant?	When the same parameter is used for both values accepted by the object and values returned from it.
Why can immutable containers often be covariant?	Because they only produce values, so accepting a subtype container is type-safe.
Why are mutable containers usually invariant?	Because they both consume and produce values, so covariance or contravariance would allow unsafe writes or reads.
When can a generic static protocol be covariant?	When its type parameter appears only in output positions, such as the return type of a protocol method.
Why can operator overloading be valuable in Python?	It can make APIs and domain-specific expressions more readable by letting user-defined types use familiar operators.
What restrictions does Python place on operator overloading?	You can only overload existing operators, you cannot change built-in types' operator meanings, and some operators such as `is`, `and`, `or`, and `not` cannot be overloaded.
What is the general rule for unary and infix operator methods in Python?	They should return a new result object and not mutate their operands.
What should an infix operator special method return if it cannot handle the operand type?	`NotImplemented`.
Why should an operator special method return `NotImplemented` instead of raising `TypeError` when it cannot handle an operand?	It lets Python try the reversed operator method on the other operand before failing.
What is the difference between `NotImplemented` and `NotImplementedError` in Python?	`NotImplemented` is a return value used in operator dispatch; `NotImplementedError` is an exception often raised by unimplemented methods.
When evaluating `a + b`, what does Python try first?	It calls `a.__add__(b)`.
When does Python try the reversed operator method such as `__radd__`?	When the forward method is missing or returns `NotImplemented`.
When is a reverse operator method like `__radd__` often simple to implement?	For a commutative operation, it can often delegate to the forward implementation.
Why is duck typing often useful in operator overloading?	It allows compatible non-matching types to interoperate as long as they support the needed operations.
Why can explicit `isinstance` checks still be useful in operator overloading?	They can make behavior and error handling more predictable than unconstrained duck typing.
Why should `__add__` not mutate its operands?	Users expect `+` to produce a result value, not to change either input object.
What does the `@` operator represent in Python?	Matrix multiplication or another matmul-like product defined by `__matmul__`.
When is it reasonable for `@` on two vector-like operands to raise `ValueError`?	When the operands are otherwise compatible but have incompatible dimensions, such as different lengths for a dot product.
How are rich comparison operators different from arithmetic operators in Python dispatch?	They use paired forward/reverse comparison methods and have special fallback behavior for `==` and `!=`.
What fallback does Python use if `==` cannot be handled by either operand?	It compares object identities.
What fallback does Python use if `!=` cannot be handled by either operand?	It negates the result of `==`.
Why is returning `NotImplemented` from `__eq__` often better than trying to compare every iterable?	It lets the other operand participate in dispatch and avoids surprising cross-type equality rules.
Why can overly permissive equality on unrelated types be a problem?	It can create surprising results and ambiguous semantics, making code harder to reason about.
How does augmented assignment work if a Python class does not implement `__iadd__`?	Python falls back to `a = a + b`, which usually creates a new object.
When should a mutable Python type implement `__iadd__`?	When `+=` should update the object in place instead of creating a new one.
What must an in-place operator method like `__iadd__` return?	`self`.
Should immutable Python types implement in-place operator methods such as `__iadd__`?	No; in-place mutation is contrary to immutability.
Why can `+=` reasonably be more permissive than `+` for mutable sequence-like types?	`+=` keeps the left-hand object's type and updates it in place, while `+` creates a new result and therefore often needs stricter operand-type rules.
When is implementing a reverse infix method like `__radd__` unnecessary?	When the forward method is intentionally defined to work only with operands of the same type, so a reversed call could never add new valid cases.
What does `iter(x)` try first in Python?	It first looks for `x.__iter__()` and uses that to obtain an iterator.
How can an object still be iterable in Python even if it does not define `__iter__`?	If it implements `__getitem__` with 0-based indexes, `iter()` can build an iterator from that legacy sequence protocol.
What is the most accurate way to check whether an object is iterable in Python?	Call `iter(x)` and handle `TypeError` if it fails.
What is an iterator in Python?	An object with `__next__` that yields items one by one and raises `StopIteration` when exhausted.
Why must a Python iterator also implement `__iter__` returning `self`?	So it can be used anywhere an iterable is expected, such as in a `for` loop.
What is the difference between an iterable and an iterator in Python?	An iterable can produce new iterators; an iterator is the stateful object currently yielding items.
Why should an iterable usually not be its own iterator?	Because each traversal should get an independent iterator state, allowing multiple or repeated passes over the same data.
What is a generator function in Python?	A function containing `yield` that returns a generator object when called.
How does `next(g)` interact with a generator object?	It resumes execution until the next `yield`, returns the yielded value, and suspends the generator again.
What happens when a generator function runs to completion?	The generator object raises `StopIteration`.
What is the difference between a list comprehension and a generator expression?	A list comprehension eagerly builds a list; a generator expression produces values lazily on demand.
Why can a lazy iterator or generator save resources?	It produces items only when needed, which can reduce memory use and avoid unnecessary work.
Why can `re.finditer` be preferable to `re.findall` for large inputs?	`finditer` yields matches lazily instead of building a full list of all matches up front.
What determines whether to use a generator expression or a generator function in Python?	Use a generator expression for short simple logic; use a generator function when the logic is complex enough that a named function is clearer.
What is the purpose of the two-argument form `iter(callable, sentinel)`?	It repeatedly calls a zero-argument callable and stops iteration when the callable returns the sentinel value.
What does `itertools.count` generate?	An arithmetic progression that continues indefinitely unless externally limited.
Why is `itertools.takewhile` useful with an infinite generator?	It stops the output as soon as the predicate becomes false, making an otherwise unbounded source finite.
What does `itertools.accumulate` do?	It yields running accumulated results, such as partial sums, minima, maxima, or products.
What does `zip` do with multiple iterables in Python?	It consumes them in parallel and stops when the shortest iterable is exhausted.
What does `itertools.zip_longest` do differently from `zip`?	It continues until the longest iterable is exhausted, filling missing positions with a default value.
What does `itertools.chain` do?	It yields all items from several iterables one after another as one continuous stream.
What does `itertools.product` compute?	The Cartesian product of the input iterables.
What does `itertools.groupby` assume about its input?	Items that belong in the same group should already be adjacent, often because the input was sorted by the grouping key.
What does `itertools.tee` do?	It creates multiple independent iterators that each yield the items of one input iterable.
Why can `all()` and `any()` be more efficient than a generic reduce?	They short-circuit and stop as soon as the final truth value is determined.
What does `yield from subgen()` do in a generator?	It delegates iteration to the subgenerator, passing its yielded values directly to the caller until the subgenerator finishes.
Why can `yield from` simplify recursive or nested generator code?	It replaces explicit loops that forward values from another generator.
What value can `yield from` capture from a subgenerator?	The value returned by the subgenerator when it finishes.
What is the difference between a generator used as an iterator and a classic coroutine?	A generator used as an iterator produces data for the caller, while a classic coroutine is mainly designed to receive data via `send`.
What does it mean to prime a classic coroutine in Python?	Advance it to its first `yield`, usually with `next(coro)` or `coro.send(None)`, so it is ready to receive sent values.
What happens if you call `send(x)` on a classic coroutine before priming it?	It fails, because the coroutine is not yet suspended at a `yield` where it can receive a value.
What does `.close()` do to a generator or classic coroutine?	It raises `GeneratorExit` inside it, causing it to terminate unless explicitly handled.
How can a classic coroutine return a final value in Python?	By using `return value`, which is delivered to the caller as the `value` attribute of the resulting `StopIteration`.
Why is `Iterator[T]` often enough as a type hint for a generator used only for iteration?	Because it conveys the yielded item type without the extra send/return details of the fuller `Generator` type.
What do the three type parameters in `Generator[YieldType, SendType, ReturnType]` represent?	The type yielded out, the type accepted by `send`, and the final return type.
Why is the `SendType` of `Generator[YieldType, SendType, ReturnType]` contravariant?	Because it describes values that go into the coroutine, so a coroutine that accepts a broader input type can stand in for one that accepts a narrower type.
What problem does the `with` statement solve (Python)?	It packages temporary setup and reliable teardown so cleanup happens even if the block exits via `return` or an exception.
What protocol makes an object usable in a `with` statement (Python)?	The context manager protocol: `__enter__` and `__exit__`.
In `with expr as target:`, what value is bound to `target` (Python)?	Whatever `expr.__enter__()` returns, which may be different from the context manager object itself.
When is `__exit__` called for a context manager (Python)?	Whenever control leaves the `with` block, whether by normal completion, `return`, or exception.
What does `__exit__` receive when a `with` block raises an exception (Python)?	The exception type, the exception instance, and the traceback object.
What does a truthy return value from `__exit__` mean (Python)?	It tells the interpreter the exception was handled and should be suppressed.
Why are context managers useful beyond automatic file closing (Python)?	They factor reusable setup/teardown or temporary state changes into a safer abstraction.
What does `contextlib.closing` provide (Python)?	A context manager wrapper for objects that have `close()` but do not implement `__enter__` and `__exit__`.
What does `contextlib.suppress` do (Python)?	It temporarily ignores specified exception types within a `with` block.
What does `contextlib.nullcontext` do (Python)?	It provides a no-op context manager, useful when code conditionally needs a real context manager or none at all.
When is `contextlib.ExitStack` especially useful (Python)?	When the number of context managers to enter is dynamic or not known in advance.
What does the `@contextmanager` decorator let you implement (Python)?	A context manager as a generator with one `yield`, instead of writing a class with `__enter__` and `__exit__`.
In a generator-based context manager, what does the code before `yield` represent (Python)?	The setup phase that runs like `__enter__`.
In a generator-based context manager, what does the code after `yield` represent (Python)?	The teardown phase that runs like `__exit__`.
Why should a `@contextmanager` generator usually put `yield` inside `try/finally` (Python)?	To guarantee teardown runs even if code inside the `with` block raises an exception.
How are exceptions from a `with` block delivered into a `@contextmanager` generator (Python)?	They are raised back into the generator at the `yield` point.
What does `|` mean inside a `match` pattern (Python)?	It forms an OR-pattern: the case matches if any alternative pattern matches.
What restriction applies to variable binding in an OR-pattern (Python)?	All alternatives must bind the same set of variables.
What is a guard in a `case` clause (Python)?	An `if` condition checked after the pattern matches to decide whether that case should run.
Why is `match/case` a good fit for interpreters or DSLs (Python)?	The patterns can mirror the grammar or tree structure of the language, making dispatch clearer and easier to extend.
When does the `else` clause of a loop run (Python `for`/`while`)?	Only if the loop completes normally without `break`; for `while`, that means the condition became false.
When does the `else` clause of a `try` statement run (Python)?	Only if the `try` block completes without raising an exception.
Why can `try/except/else` be clearer than putting follow-up code inside `try` (Python)?	It keeps only the operation expected to fail inside `try` and makes the success-only path explicit.
What does EAFP mean in Python?	"Easier to ask forgiveness than permission": do the operation and handle exceptions if needed.
Why can EAFP be safer than LBYL in concurrent code (Python)?	State can change between a precheck and the action, but exception handling checks at the actual point of use.
What is concurrency in Python systems?	The ability to handle multiple pending tasks so each eventually makes progress, even if only one runs at a time.
What is parallelism in Python systems?	The ability to execute multiple computations at the same time, typically using multiple CPU cores or machines.
What is an execution unit in Python concurrency?	A process, thread, or coroutine: something that can execute code independently.
What is a process in Python concurrency?	A separate running program with its own memory space and interpreter state.
Why is communication between Python processes relatively costly?	Objects must be serialized to bytes to cross process boundaries.
What is a thread in Python concurrency?	An execution unit inside one process that shares the process memory space with other threads.
What is the main advantage of Python threads over processes?	Threads are lighter weight and can share live objects directly.
What is the main risk of shared-memory threads in Python?	Concurrent mutation of the same objects can corrupt data unless access is synchronized.
What is a coroutine in Python concurrency?	A function that can suspend itself and resume later, usually under control of an event loop.
What kind of multitasking do Python coroutines use?	Cooperative multitasking: a coroutine must explicitly yield control with `await` or `yield`.
What kind of multitasking do Python threads and processes use?	Preemptive multitasking under the operating system scheduler.
What is a queue in concurrent Python programs?	A structure for exchanging data and control messages between execution units, often in FIFO order.
What is a lock or mutex in Python concurrency?	A synchronization object used to prevent multiple execution units from updating shared state at the same time.
What is contention in concurrent systems?	Competition among execution units for limited resources such as CPU time, locks, or I/O.
Why are threads or processes harder to manage than ordinary function calls?	Completion, results, errors, and shutdown usually require explicit coordination instead of direct return values and exceptions.
Why are long-lived worker threads or processes common in concurrent programs?	They amortize startup cost by processing many jobs instead of doing one task and exiting.
What is the Global Interpreter Lock (GIL) in CPython?	A lock protecting interpreter state so only one thread executes Python bytecode at a time.
Why can Python threads still work well for I/O-bound tasks despite the GIL?	Blocking I/O operations release the GIL, and I/O latency dominates execution time anyway.
Why are Python threads a poor fit for CPU-bound work?	Threads contend for the GIL and add context-switching overhead instead of running Python code in parallel.
How do you use multiple CPU cores for CPU-bound Python code?	Run the work in multiple processes, such as with `multiprocessing` or a process pool.
When does CPython automatically release the GIL?	During many blocking system calls such as disk I/O, network I/O, and `time.sleep()`.
Why doesn’t the GIL usually matter for coroutines by default?	Coroutines normally share one thread and one event loop, so they are not competing for parallel execution of Python bytecode.
What is `threading.Event` used for in Python?	Simple signalling between threads, such as telling a worker thread to stop.
What does `Event.wait(timeout)` do in Python threading?	It blocks until the event is set or until the timeout expires.
How does the `multiprocessing` API relate to `threading`?	It intentionally mirrors much of the threading API, but runs work in separate processes.
Why can `multiprocessing` bypass the GIL?	Each child process has its own Python interpreter and its own GIL.
What does `asyncio.run(coro())` do?	It starts an event loop, runs the coroutine to completion, and returns its result.
What does `asyncio.create_task(coro())` do?	It schedules a coroutine to run soon and returns a `Task` that manages it.
What does `await some_coro()` do in Python?	It suspends the current coroutine until the awaited operation completes, yielding control to the event loop.
Why should `asyncio` code use `await asyncio.sleep(...)` instead of `time.sleep(...)`?	`asyncio.sleep` yields control without blocking the event loop, while `time.sleep` blocks the whole thread.
What happens if a CPU-bound function runs directly inside a coroutine?	It blocks the event loop, preventing other coroutines from making progress.
What does `await asyncio.sleep(0)` do?	It yields control to the event loop immediately so other pending coroutines can run.
Why is `await asyncio.sleep(0)` only a stopgap for CPU-bound async code?	The CPU-heavy work still runs in the event-loop thread between yields, slowing the whole program.
What is the rough asyncio equivalent of a `threading.Thread`?	An `asyncio.Task`, which drives a coroutine.
How is coroutine cancellation delivered in `asyncio`?	By raising `CancelledError` at an await point inside the coroutine.
Why are queues central to process-based worker pools in Python?	They let the parent submit jobs and collect results without shared mutable state.
What is a poison pill or sentinel in a worker queue?	A special value placed in the queue to tell a worker to stop processing and exit.
Why is `object()` a poor sentinel for inter-process queues in Python?	After serialization and deserialization, object identity is not preserved.
Why can results from multiple worker processes arrive out of submission order?	Different jobs finish at different times when workers run in parallel.
Why can many CPU-bound Python threads be slower than one sequential thread?	GIL contention and context-switch overhead can outweigh any benefit.
What problem do WSGI application servers solve for Python web apps?	They distribute requests across multiple Python worker processes so sequential app code can use multicore machines.
What is the difference between WSGI and ASGI?	WSGI is a synchronous server interface; ASGI is designed to support asynchronous applications and coroutines.
What is a distributed task queue in Python web architecture?	A system that sends background jobs to separate worker processes or machines via a message queue.
Why are distributed task queues useful in web applications?	They offload slow work such as email, PDF generation, retries, and scheduled jobs from request-handling processes.
Why does a producer-consumer task-queue design improve scalability?	Producers and workers are decoupled, so you can add more workers without changing the producers.
